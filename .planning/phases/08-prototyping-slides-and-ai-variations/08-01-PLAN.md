---
phase: 8
plan: 01
title: Core Prototype, Slide, Variation, and Agent Recipe Model
wave: 1
depends_on: []
files_modified:
  - packages/editor-core/src/schemas.ts
  - packages/editor-core/src/prototype.ts
  - packages/editor-core/src/slides.ts
  - packages/editor-core/src/variations.ts
  - packages/editor-core/src/handoff.ts
  - packages/editor-core/src/index.ts
  - packages/editor-core/src/__tests__/prototype.test.ts
  - packages/editor-core/src/__tests__/slides.test.ts
  - packages/editor-core/src/__tests__/variations.test.ts
  - packages/editor-core/src/__tests__/schemas.test.ts
  - packages/editor-core/src/__tests__/handoff.test.ts
requirements_addressed:
  - AI-05
  - PROTO-01
  - PROTO-02
  - PROTO-03
  - SLIDE-01
  - SLIDE-02
  - SLIDE-03
foundation_only:
  - AI-01
  - AI-02
  - AI-03
  - AI-04
gap_closure: false
---

<objective>
Add the stored-state Phase 08 foundation: typed prototype interactions, component variables/state rules, non-mutating presentation preview state, slide decks with notes/embed/feedback primitives, selected-region variation sets, and portable canvas-aware agent recipes.

Complexity: L
Requirements completed: AI-05, PROTO-01, PROTO-02, PROTO-03, SLIDE-01, SLIDE-02, SLIDE-03
Foundation-only requirements: AI-01, AI-02, AI-03, AI-04. These require a real canvas-aware agent/model-generated typed operation path before they can be marked complete.
Phase goal contribution: This gives prototypes, slides, variations, and agent recipes durable schemas and deterministic helpers before the web UI exposes them.
</objective>

<tasks>
<task id="8-01-01" type="auto">
  <name>Add Phase 08 schemas to ProjectBundle</name>
  <files>
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/__tests__/schemas.test.ts
  </files>
  <read_first>
    - CLAUDE.md
    - .planning/PRODUCT-NORTH-STAR.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-CONTEXT.md
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-RESEARCH.md
    - .planning/research/COMPETITIVE-GAP-REVIEW.md
    - docs/guides/coding-principles.md
    - docs/prompts/context-driven-design-agent-prompt.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/__tests__/schemas.test.ts
  </read_first>
  <action>
    Extend `packages/editor-core/src/schemas.ts` with exported schemas and types:
    - `PrototypeTriggerSchema = z.enum(["click", "hover", "tap", "keyboard", "timed"])`
    - `PrototypeActionKindSchema = z.enum(["navigateTo", "setComponentState", "setVariable", "toggleVariable", "openOverlay", "closeOverlay"])`
    - `PrototypeTransitionSchema` with `kind: "instant" | "dissolve" | "slide"`, optional `durationMs`, optional `easing`
    - `PrototypeConditionSchema` with `variableId`, `operator: "equals" | "notEquals" | "isTruthy" | "isFalsy"`, optional `value`
    - `PrototypeInteractionSchema` with `id`, `sourceObjectId`, `trigger`, `action`, optional `targetObjectId`, optional `variableId`, optional `value`, optional `key`, optional `delayMs`, optional `conditions`, optional `transition`, `provenance`, and `createdAt`
    - `PrototypeVariableSchema` with `id`, `name`, `kind: "string" | "number" | "boolean" | "mode"`, `defaultValue`, optional `modeValues`, and optional `sharedComponentId`
    - `ComponentStateRuleSchema` with `id`, `componentId`, optional `variantId`, `state`, `variableBindings`, and optional `conditions`
    - `PrototypeGraphSchema` with `version: 1`, `interactions`, `variables`, `stateRules`, and `updatedAt`
    - `PresentationStateSchema` with `mode: "edit" | "present"`, optional `activeObjectId`, `activeSlideId`, `activeInteractionId`, `variableValues`, `componentStates`, `history`, and `startedAt`
    - `SlideDeckSchema`, `SlideSchema`, `SlideBlockSchema`, and `SlideFeedbackSchema` for slide view/grid view/outline/notes, embedded canvas or prototype blocks, comments, polls, votes, and alignment-scale feedback
    - `VariationSetSchema`, `VariationDirectionSchema`, and `AgentRecipeSchema` for selected-region remix/compare/promote and portable replayable recipes

    Extend `ProjectBundleSchema` with defaulted fields:
    - `prototypeGraph: PrototypeGraphSchema.optional()`
    - `presentationState: PresentationStateSchema.optional()`
    - `slideDecks: z.array(SlideDeckSchema).default([])`
    - `variationSets: z.array(VariationSetSchema).default([])`
    - `agentRecipes: z.array(AgentRecipeSchema).default([])`

    Existing bundles without these fields must still parse.

    Update schema tests:
    - legacy bundle fixtures parse with empty/default Phase 08 arrays.
    - a bundle containing prototype graph, presentation state, slide decks, variation sets, and agent recipes parses.
    - invalid trigger, invalid slide block kind, invalid feedback kind, and invalid recipe runtime fail.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/schemas.ts contains "export const PrototypeInteractionSchema"
    - packages/editor-core/src/schemas.ts contains "export const PrototypeGraphSchema"
    - packages/editor-core/src/schemas.ts contains "export const SlideDeckSchema"
    - packages/editor-core/src/schemas.ts contains "export const VariationSetSchema"
    - packages/editor-core/src/schemas.ts contains "export const AgentRecipeSchema"
    - packages/editor-core/src/schemas.ts contains "slideDecks: z.array(SlideDeckSchema).default([])"
    - packages/editor-core/src/__tests__/schemas.test.ts contains "parses legacy bundles with prototype slide and variation defaults"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/schemas.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="8-01-02" type="auto">
  <name>Add prototype interaction and presentation helpers</name>
  <files>
    - packages/editor-core/src/prototype.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/prototype.test.ts
  </files>
  <read_first>
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-CONTEXT.md
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-RESEARCH.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/canvas-graph.ts
    - packages/editor-core/src/canvas-components.ts
    - packages/editor-core/src/canvas-operations.ts
    - packages/editor-core/src/__tests__/canvas-components.test.ts
    - packages/editor-core/src/__tests__/canvas-graph.test.ts
  </read_first>
  <action>
    Create `packages/editor-core/src/prototype.ts` with exported helpers:
    - `ensurePrototypeGraph(bundle: ProjectBundle, createdAt?: string): ProjectBundle`
    - `addPrototypeVariable(bundle: ProjectBundle, input): ProjectBundle`
    - `addPrototypeInteraction(bundle: ProjectBundle, input): ProjectBundle`
    - `addComponentStateRule(bundle: ProjectBundle, input): ProjectBundle`
    - `createPresentationState(bundle: ProjectBundle, input): PresentationState`
    - `playPrototypeInteraction(bundle: ProjectBundle, state: PresentationState, interactionId: string): PresentationState`

    Validation requirements:
    - source and target object ids must exist in `canvasGraph.objects`.
    - `keyboard` trigger must include a non-empty safe key string.
    - `timed` trigger must include `delayMs > 0`.
    - `setVariable` and `toggleVariable` actions must reference an existing variable.
    - component state rules must reference existing components and variants.
    - presentation state helpers must not mutate `ProjectBundle`, `CanvasGraph`, `canvasOperations`, or `patches`.

    Export `./prototype.js` from `packages/editor-core/src/index.ts`.

    Unit tests must cover click, hover, tap, keyboard, and timed interactions; variable conditions; component state rules; presentation playback; unknown object ids; unknown variables; invalid keyboard/timed input; and bundle non-mutation.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/prototype.ts exists
    - packages/editor-core/src/prototype.ts contains "export function addPrototypeInteraction("
    - packages/editor-core/src/prototype.ts contains "export function playPrototypeInteraction("
    - packages/editor-core/src/index.ts contains "export * from \"./prototype.js\""
    - packages/editor-core/src/__tests__/prototype.test.ts contains "creates click hover tap keyboard and timed interactions"
    - packages/editor-core/src/__tests__/prototype.test.ts contains "previews prototype interactions without mutating the bundle"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/prototype.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="8-01-03" type="auto">
  <name>Add slide deck and feedback helpers</name>
  <files>
    - packages/editor-core/src/slides.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/slides.test.ts
  </files>
  <read_first>
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-CONTEXT.md
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-RESEARCH.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/canvas-graph.ts
    - packages/editor-core/src/__tests__/schemas.test.ts
  </read_first>
  <action>
    Create `packages/editor-core/src/slides.ts` with exported helpers:
    - `createSlideDeck(bundle: ProjectBundle, input): ProjectBundle`
    - `addSlide(bundle: ProjectBundle, deckId: string, input): ProjectBundle`
    - `setSlideNotes(bundle: ProjectBundle, deckId: string, slideId: string, notes: string): ProjectBundle`
    - `addSlideBlock(bundle: ProjectBundle, deckId: string, slideId: string, input): ProjectBundle`
    - `addSlideFeedback(bundle: ProjectBundle, deckId: string, slideId: string, input): ProjectBundle`
    - `setSlideDeckView(bundle: ProjectBundle, deckId: string, view: "slide" | "grid" | "outline"): ProjectBundle`

    Validation requirements:
    - deck and slide ids must exist.
    - embedded canvas/prototype blocks must reference existing canvas objects or prototype interactions.
    - notes must be bounded to a reasonable length such as 5000 characters.
    - polls must include 2-6 choices.
    - alignment-scale feedback must use an integer value in range 1-5.
    - vote targets must reference an existing poll or slide block.

    Export `./slides.js` from `packages/editor-core/src/index.ts`.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/slides.ts exists
    - packages/editor-core/src/slides.ts contains "export function createSlideDeck("
    - packages/editor-core/src/slides.ts contains "export function addSlideBlock("
    - packages/editor-core/src/slides.ts contains "export function addSlideFeedback("
    - packages/editor-core/src/index.ts contains "export * from \"./slides.js\""
    - packages/editor-core/src/__tests__/slides.test.ts contains "creates a deck with outline notes embedded blocks and feedback"
    - packages/editor-core/src/__tests__/slides.test.ts contains "rejects invalid slide feedback"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/slides.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="8-01-04" type="auto">
  <name>Add selected-region variation and portable agent recipe helpers</name>
  <files>
    - packages/editor-core/src/variations.ts
    - packages/editor-core/src/handoff.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/variations.test.ts
    - packages/editor-core/src/__tests__/handoff.test.ts
  </files>
  <read_first>
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-CONTEXT.md
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-RESEARCH.md
    - docs/prompts/context-driven-design-agent-prompt.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/canvas-operations.ts
    - packages/editor-core/src/patches.ts
    - packages/editor-core/src/handoff.ts
    - packages/editor-core/src/__tests__/handoff.test.ts
  </read_first>
  <action>
    Create `packages/editor-core/src/variations.ts` with exported helpers:
    - `createVariationSet(bundle: ProjectBundle, input): ProjectBundle`
    - `addVariationDirection(bundle: ProjectBundle, variationSetId: string, input): ProjectBundle`
    - `createSelectedRegionRemix(bundle: ProjectBundle, input): ProjectBundle`
    - `promoteVariationDirection(bundle: ProjectBundle, variationSetId: string, directionId: string): ProjectBundle`
    - `exportAgentRecipe(bundle: ProjectBundle, input): ProjectBundle`

    Requirements:
    - variation sets must target a selected canvas object id.
    - localized remix directions must preserve surrounding layout by storing typed `CanvasOperation` or `EditPatch` records scoped to the target object/node only.
    - stale `baseRevision`, unknown object ids, unknown node ids, raw HTML operations, and live iframe DOM references must be rejected.
    - promoting a direction appends its typed operations to stored state and records provenance.
    - agent recipes must include runtime target, source revision, target object id, prompt, typed operation ids, design prompt path, and replay instructions.
    - recipe paths must remain repo-relative and must not include `..`, absolute paths, or `://`.

    Update `handoff.ts` so runtime-agnostic handoff packages include `prototypeGraph`, `slideDecks`, `variationSets`, and `agentRecipes`.
    Export `./variations.js` from `packages/editor-core/src/index.ts`.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/variations.ts exists
    - packages/editor-core/src/variations.ts contains "export function createSelectedRegionRemix("
    - packages/editor-core/src/variations.ts contains "export function promoteVariationDirection("
    - packages/editor-core/src/variations.ts contains "export function exportAgentRecipe("
    - packages/editor-core/src/handoff.ts contains "variationSets"
    - packages/editor-core/src/handoff.ts contains "agentRecipes"
    - packages/editor-core/src/__tests__/variations.test.ts contains "creates compares promotes and exports selected-region variation recipes"
    - packages/editor-core/src/__tests__/variations.test.ts contains "rejects stale unsafe or cross-region variation operations"
    - packages/editor-core/src/__tests__/handoff.test.ts contains "prototypeGraph"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/variations.test.ts src/__tests__/handoff.test.ts exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] Legacy bundles parse with default Phase 08 fields.
- [ ] Prototype interactions support click, hover, tap, keyboard, and timed triggers as typed records.
- [ ] Component state rules support variables, conditions, variants, and shared component state metadata.
- [ ] Presentation playback produces preview state without mutating bundle source state.
- [ ] Slide decks support slide/grid/outline view state, notes, embedded canvas/prototype blocks, comments, polls, votes, and alignment-scale feedback.
- [ ] Selected-region remix creates side-by-side variation directions scoped to the selected canvas object.
- [ ] Promoting a variation applies stored typed operations and records provenance.
- [ ] Agent recipes export portable prompt/script replay metadata for runtime-agnostic agents.
- [ ] Unknown objects, unknown variables, stale revisions, unsafe recipe paths, invalid feedback, raw HTML operations, and live DOM references are rejected by tests.
- [ ] Handoff packages include prototype, slide, variation, and recipe state.
- [ ] All task acceptance criteria verified
- [ ] /sunco:lint passes with zero errors
- [ ] npx tsc --noEmit passes with zero errors
- [ ] pnpm lint exits 0
- [ ] pnpm typecheck exits 0
- [ ] pnpm test exits 0
</done_when>
