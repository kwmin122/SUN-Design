---
phase: 8
plan: 03
title: Canvas-Aware Agent Output Ingestion Gap Closure
wave: 1
depends_on: []
files_modified:
  - packages/editor-core/src/schemas.ts
  - packages/editor-core/src/agent-output.ts
  - packages/editor-core/src/variations.ts
  - packages/editor-core/src/integrity.ts
  - packages/editor-core/src/index.ts
  - packages/editor-core/src/__tests__/agent-output.test.ts
  - packages/editor-core/src/__tests__/persistence.test.ts
  - apps/web/components/variation-compare-panel.tsx
  - apps/web/components/editor-shell.tsx
  - apps/web/app/globals.css
  - apps/web/tests/phase-08-agent-output.spec.ts
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/08-prototyping-slides-and-ai-variations/08-VERIFICATION.md
requirements_addressed:
  - AI-01
  - AI-02
  - AI-03
  - AI-04
gap_closure: true
---

<objective>
Close the Phase 08 ship hold by replacing the deterministic-only selected-region remix claim with a real local-first, agent-agnostic ingestion path: selected canvas context package -> external agent/model structured output -> validated typed operations/patches -> side-by-side compare -> explicit promote. This plan does not add hosted model billing, auth, worker queues, live data ingestion, or Dev Mode export surfaces.

Complexity: L
Requirements fulfilled: AI-01, AI-02, AI-03, AI-04
Phase goal contribution: Completes the remaining localized AI variation requirements while preserving the Phase 01/06/08 source-of-truth rule that stored `ProjectBundle` records and typed operations, not live iframe DOM, own the artifact.
</objective>

<tasks>
<task id="8-03-01" type="auto">
  <name>Add agent context and structured output schemas</name>
  <files>
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/agent-output.ts
    - packages/editor-core/src/index.ts
  </files>
  <read_first>
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-CONTEXT.md
    - .planning/PRODUCT-NORTH-STAR.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-VERIFICATION.md
    - docs/prompts/context-driven-design-agent-prompt.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/variations.ts
    - packages/editor-core/src/integrity.ts
    - packages/editor-core/src/index.ts
  </read_first>
  <action>
    Extend `packages/editor-core/src/schemas.ts` with durable, reloadable agent-output records:

    - `AgentOutputDiagnosticSchema = z.object({ id: z.string().min(1), severity: z.enum(["info", "warning", "error"]), code: z.enum(["parse-error", "schema-error", "stale-revision", "missing-reference", "out-of-scope-target", "unsafe-patch", "unsupported-operation", "insufficient-directions", "runtime-mismatch"]), message: z.string().min(1), targetId: z.string().min(1).optional(), createdAt: z.string().min(1) })`
    - `AgentContextObjectSummarySchema = z.object({ id: z.string().min(1), kind: CanvasObjectKindSchema, name: z.string().min(1), nodeId: z.string().min(1).optional(), parentId: z.string().min(1).optional(), childIds: z.array(z.string().min(1)).default([]) })`
    - `AgentContextPackageSchema = z.object({ id: z.string().min(1), runtime: AgentRuntimeSchema.optional(), targetObjectId: z.string().min(1), sourceRevision: z.string().min(1), prompt: z.string().min(1), instructionsPath: z.string().refine((path) => path.startsWith("docs/prompts/") && path.endsWith(".md") && !path.includes("..") && !path.includes("://") && !path.startsWith("/"), "instructionsPath must be a safe repo-relative prompt markdown path"), selectedObject: AgentContextObjectSummarySchema, ancestors: z.array(AgentContextObjectSummarySchema).default([]), siblings: z.array(AgentContextObjectSummarySchema).default([]), tokenSummary: z.array(z.string().min(1)).default([]), guardrails: z.array(z.string().min(1)).default([]), createdAt: z.string().min(1) })`
    - `AgentOutputDirectionSchema = z.object({ id: z.string().min(1), name: z.string().min(1), description: z.string().min(1), rationale: z.string().min(1), targetObjectId: z.string().min(1), operations: z.array(CanvasOperationSchema).default([]), patches: z.array(EditPatchSchema).default([]), provenance: z.string().min(1), createdAt: z.string().min(1) })`
    - `AgentOutputEnvelopeSchema = z.object({ id: z.string().min(1), contextPackageId: z.string().min(1), runtime: AgentRuntimeSchema, targetObjectId: z.string().min(1), sourceRevision: z.string().min(1), prompt: z.string().min(1), directions: z.array(AgentOutputDirectionSchema).min(2).max(6), diagnostics: z.array(AgentOutputDiagnosticSchema).default([]), createdAt: z.string().min(1) })`
    - `AgentRunSchema = z.object({ id: z.string().min(1), runtime: AgentRuntimeSchema, status: z.enum(["draft", "validated", "rejected", "promoted"]), contextPackageId: z.string().min(1), outputId: z.string().min(1).optional(), targetObjectId: z.string().min(1), sourceRevision: z.string().min(1), diagnostics: z.array(AgentOutputDiagnosticSchema).default([]), createdAt: z.string().min(1), updatedAt: z.string().min(1) })`

    Extend `ProjectBundleSchema` with:

    - `agentContextPackages: z.array(AgentContextPackageSchema).default([])`
    - `agentOutputs: z.array(AgentOutputEnvelopeSchema).default([])`
    - `agentRuns: z.array(AgentRunSchema).default([])`

    Create `packages/editor-core/src/agent-output.ts` exporting:

    - `createAgentContextPackage(bundle: ProjectBundle, input: { targetObjectId: string; prompt: string; runtime?: AgentRuntime; instructionsPath?: string; createdAt?: string }): ProjectBundle`
    - `ingestAgentOutput(bundle: ProjectBundle, input: { contextPackageId: string; runtime: AgentRuntime; output: unknown; createdAt?: string }): ProjectBundle`
    - `parseAgentOutputJson(json: string): unknown`

    `createAgentContextPackage` must:
    - call `ensureCanvasGraph(bundle)`.
    - require the target canvas object to exist.
    - summarize the selected object, ancestor chain, and sibling objects as `AgentContextObjectSummary`.
    - set `sourceRevision` to `bundle.baseRevision`.
    - set `instructionsPath` to `docs/prompts/context-driven-design-agent-prompt.md` by default.
    - include guardrail strings exactly: `"Use stored ProjectBundle ids only."`, `"Do not read or save live iframe DOM."`, `"Return typed CanvasOperation[] and EditPatch[] only."`, `"Preserve surrounding layout outside targetObjectId."`

    `ingestAgentOutput` must:
    - parse `AgentOutputEnvelopeSchema`.
    - require `output.contextPackageId` to equal the selected context package id.
    - require `input.runtime` to equal `output.runtime`; a mismatch must persist a rejected `AgentRun` with diagnostic code `runtime-mismatch` and must not create variation directions.
    - require `output.sourceRevision` to equal the context package source revision.
    - require every direction target id to equal the context package `targetObjectId`.
    - require at least two output directions for successful ingestion; a one-direction output must persist a rejected `AgentRun` with diagnostic code `insufficient-directions` or `schema-error` and must not create variation directions.
    - create or reuse a `VariationSet` for the context package prompt and target object.
    - add one `VariationDirection` per output direction with `status: "validated"` and `provenance: "agent-output:<runtime>"`.
    - persist `agentOutputs` and `agentRuns` with run status `validated` when ingestion succeeds and `rejected` with diagnostics when parse/schema/scope validation fails.

    Export the new module from `packages/editor-core/src/index.ts` using `export * from "./agent-output.js";`.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/schemas.ts contains "export const AgentContextPackageSchema"
    - packages/editor-core/src/schemas.ts contains "export const AgentOutputEnvelopeSchema"
    - packages/editor-core/src/schemas.ts contains "export const AgentRunSchema"
    - packages/editor-core/src/schemas.ts contains "directions: z.array(AgentOutputDirectionSchema).min(2).max(6)"
    - packages/editor-core/src/schemas.ts contains "runtime-mismatch"
    - packages/editor-core/src/schemas.ts contains "insufficient-directions"
    - packages/editor-core/src/schemas.ts contains "agentContextPackages: z.array(AgentContextPackageSchema).default([])"
    - packages/editor-core/src/agent-output.ts exists
    - packages/editor-core/src/agent-output.ts contains "export function createAgentContextPackage("
    - packages/editor-core/src/agent-output.ts contains "export function ingestAgentOutput("
    - packages/editor-core/src/agent-output.ts contains "export function parseAgentOutputJson("
    - packages/editor-core/src/index.ts contains "export * from \"./agent-output.js\""
    - pnpm --filter @kdesign/editor-core typecheck exits 0
  </acceptance_criteria>
</task>

<task id="8-03-02" type="auto">
  <name>Validate agent output and close graph corruption paths</name>
  <files>
    - packages/editor-core/src/agent-output.ts
    - packages/editor-core/src/integrity.ts
    - packages/editor-core/src/variations.ts
    - packages/editor-core/src/__tests__/agent-output.test.ts
    - packages/editor-core/src/__tests__/persistence.test.ts
  </files>
  <read_first>
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-CONTEXT.md
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-VERIFICATION.md
    - docs/guides/coding-principles.md
    - packages/editor-core/src/agent-output.ts
    - packages/editor-core/src/integrity.ts
    - packages/editor-core/src/variations.ts
    - packages/editor-core/src/canvas-operations.ts
    - packages/editor-core/src/patches.ts
    - packages/editor-core/src/__tests__/variations.test.ts
    - packages/editor-core/src/__tests__/persistence.test.ts
  </read_first>
  <action>
    Extend `packages/editor-core/src/integrity.ts` so `assertProjectBundleIntegrity(bundle)` validates the new agent records. Create private helpers named exactly `assertAgentContextPackageIntegrity`, `assertAgentOutputIntegrity`, and `assertAgentRunIntegrity`, then call them from the existing project-bundle integrity flow.

    - every `agentContextPackages[].targetObjectId` exists in `bundle.canvasGraph.objects`.
    - every context package `selectedObject.id` equals `targetObjectId`.
    - every `agentOutputs[].contextPackageId` references an existing context package.
    - every `agentOutputs[].targetObjectId` equals the referenced context package `targetObjectId`.
    - every `agentOutputs[].sourceRevision` equals the referenced context package `sourceRevision`.
    - every output direction target equals `agentOutputs[].targetObjectId`.
    - every output operation `objectId` equals the target object and `baseRevision` equals `sourceRevision`.
    - every output patch `nodeId` equals the selected canvas object's `nodeId` and `baseRevision` equals `sourceRevision`.
    - every `agentRuns[].contextPackageId` references an existing context package.
    - every `agentRuns[].outputId`, when present, references an existing output envelope.

    Add a helper inside `agent-output.ts` named `validateAgentOutputScope(bundle, contextPackage, output)` that returns `AgentOutputDiagnostic[]`. It must produce diagnostic codes from `AgentOutputDiagnosticSchema` instead of throwing for normal model mistakes. `ingestAgentOutput` may throw only for missing context package or impossible internal state; model-output failures should persist a rejected `AgentRun` with diagnostics and leave `variationSets` unchanged.

    `validateAgentOutputScope` must use a strict allowlist for agent-generated selected-region changes:

    - Allowed `CanvasOperation.op`: `setLayoutConstraints` only.
    - Allowed `EditPatch.op`: `setText` and `setStyle` only.
    - Any other `CanvasOperation.op` or `EditPatch.op` must create an `unsupported-operation` diagnostic.
    - Any patch value containing raw HTML markup, event-handler attributes, `javascript:`, `expression(`, or `url(` must create an `unsafe-patch` diagnostic.
    - Any accepted operation must keep `objectId` equal to the selected target object and `baseRevision` equal to the context package `sourceRevision`.
    - Any accepted patch must keep `nodeId` equal to the selected canvas object's `nodeId` and `baseRevision` equal to the context package `sourceRevision`.

    Update `packages/editor-core/src/variations.ts` so `VariationDirectionSchema` accepts `status: "validated"` for agent-generated directions. Preserve all existing `"candidate"`, `"promoted"`, and `"rejected"` behavior and update tests that assert the old deterministic path.

    Create `packages/editor-core/src/__tests__/agent-output.test.ts` with these tests:

    - `creates a selected-region agent context package from stored canvas state`
    - `ingests agent generated directions as validated variation candidates`
    - `records rejected agent runs for stale revisions`
    - `records rejected agent runs for single-direction outputs`
    - `records rejected agent runs for runtime mismatches`
    - `records rejected agent runs for cross-target operations`
    - `rejects unsupported operations and raw html patch output`
    - `promotes an ingested agent direction through typed operations`

    Extend `packages/editor-core/src/__tests__/persistence.test.ts` with corrupt persisted state checks for:

    - missing `agentContextPackages[].targetObjectId`.
    - `agentOutputs[]` pointing at a missing context package.
    - `agentRuns[]` pointing at a missing output id.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/integrity.ts contains "function assertAgentContextPackageIntegrity"
    - packages/editor-core/src/integrity.ts contains "function assertAgentOutputIntegrity"
    - packages/editor-core/src/integrity.ts contains "function assertAgentRunIntegrity"
    - packages/editor-core/src/agent-output.ts contains "function validateAgentOutputScope("
    - packages/editor-core/src/agent-output.ts contains "const ALLOWED_AGENT_CANVAS_OPS"
    - packages/editor-core/src/agent-output.ts contains "const ALLOWED_AGENT_PATCH_OPS"
    - packages/editor-core/src/__tests__/agent-output.test.ts exists
    - packages/editor-core/src/__tests__/agent-output.test.ts contains "ingests agent generated directions as validated variation candidates"
    - packages/editor-core/src/__tests__/agent-output.test.ts contains "records rejected agent runs for stale revisions"
    - packages/editor-core/src/__tests__/agent-output.test.ts contains "records rejected agent runs for single-direction outputs"
    - packages/editor-core/src/__tests__/agent-output.test.ts contains "records rejected agent runs for runtime mismatches"
    - packages/editor-core/src/__tests__/agent-output.test.ts contains "records rejected agent runs for cross-target operations"
    - packages/editor-core/src/__tests__/agent-output.test.ts contains "rejects unsupported operations and raw html patch output"
    - packages/editor-core/src/__tests__/persistence.test.ts contains "rejects persisted agent output references"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/agent-output.test.ts src/__tests__/persistence.test.ts src/__tests__/variations.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="8-03-03" type="auto">
  <name>Expose selected-region agent ingestion in the web workflow</name>
  <files>
    - apps/web/components/variation-compare-panel.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
    - apps/web/tests/phase-08-agent-output.spec.ts
  </files>
  <read_first>
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-CONTEXT.md
    - .planning/PRODUCT-NORTH-STAR.md
    - docs/prompts/context-driven-design-agent-prompt.md
    - packages/editor-core/src/agent-output.ts
    - packages/editor-core/src/variations.ts
    - apps/web/components/variation-compare-panel.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/tests/phase-08-variations.spec.ts
  </read_first>
  <action>
    Extend `apps/web/components/variation-compare-panel.tsx` to support the local-first agent output ingestion workflow.

    Add props:

    - `onCreateAgentContext(input: { runtime: AgentRuntime; targetObjectId: string; prompt: string }): void`
    - `onIngestAgentOutput(input: { contextPackageId: string; runtime: AgentRuntime; outputJson: string }): void`

    Add visible controls with these exact test ids:

    - `data-testid="create-agent-context-button"` on a button labelled `Create agent context`
    - `data-testid="agent-context-list"` on a list that displays the most recent context package id, runtime, selected object name, and `docs/prompts/context-driven-design-agent-prompt.md`
    - `data-testid="agent-output-json-input"` on a textarea labelled `Paste agent output JSON`
    - `data-testid="ingest-agent-output-button"` on a button labelled `Ingest agent output`
    - `data-testid="agent-output-diagnostics"` on a diagnostics block that displays diagnostic codes such as `validated`, `stale-revision`, `out-of-scope-target`, and `unsafe-patch`

    The direction list must display agent-generated direction names and operation counts from ingested output. Do not hide deterministic directions, but label deterministic provenance as `deterministic-local-remix` and agent provenance as `agent-output:<runtime>`.

    Wire `apps/web/components/editor-shell.tsx`:

    - import `createAgentContextPackage`, `ingestAgentOutput`, and `parseAgentOutputJson` from `@kdesign/editor-core`.
    - implement `handleCreateAgentContext` by committing `createAgentContextPackage(bundle, input)` through the same bundle update path used by Phase 08 panels.
    - implement `handleIngestAgentOutput` by parsing JSON, calling `ingestAgentOutput`, committing the returned bundle, and surfacing rejected run diagnostics in the existing diagnostics/quality message area.
    - keep the existing `Create localized remix`, `Promote variation`, and `Export agent recipe` controls working.

    Add CSS in `apps/web/app/globals.css` for:

    - `.agent-context-list`
    - `.agent-output-json`
    - `.agent-output-diagnostics`
    - `.variation-direction-card[data-provenance^="agent-output"]`

    The CSS must preserve tablet and mobile no-overflow behavior.
  </action>
  <acceptance_criteria>
    - apps/web/components/variation-compare-panel.tsx contains "create-agent-context-button"
    - apps/web/components/variation-compare-panel.tsx contains "agent-context-list"
    - apps/web/components/variation-compare-panel.tsx contains "agent-output-json-input"
    - apps/web/components/variation-compare-panel.tsx contains "ingest-agent-output-button"
    - apps/web/components/variation-compare-panel.tsx contains "agent-output-diagnostics"
    - apps/web/components/editor-shell.tsx contains "createAgentContextPackage"
    - apps/web/components/editor-shell.tsx contains "ingestAgentOutput"
    - apps/web/components/editor-shell.tsx contains "parseAgentOutputJson"
    - apps/web/app/globals.css contains ".agent-context-list"
    - apps/web/app/globals.css contains ".agent-output-diagnostics"
    - pnpm --filter @kdesign/web typecheck exits 0
  </acceptance_criteria>
</task>

<task id="8-03-04" type="auto">
  <name>Verify the end-to-end agent-generated selected-region loop and update completion docs</name>
  <files>
    - apps/web/tests/phase-08-agent-output.spec.ts
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-CONTEXT.md
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - apps/web/tests/phase-08-variations.spec.ts
    - apps/web/tests/phase-08-prototype.spec.ts
    - apps/web/tests/phase-08-slides.spec.ts
  </read_first>
  <action>
    Create `apps/web/tests/phase-08-agent-output.spec.ts` with a Playwright test named exactly `ingests agent generated selected-region directions`.

    The test must:

    - open a fresh project and select a canvas object in the sandbox iframe.
    - fill `localized-remix-prompt` with Korean text that asks for a selected-region restyle.
    - click `create-agent-context-button`.
    - read localStorage key `kdesign.phase01.project.v1`, parse the latest `agentContextPackages[0]`, and assert it includes `targetObjectId`, `sourceRevision`, `selectedObject`, `guardrails`, and `docs/prompts/context-driven-design-agent-prompt.md`.
    - build an `AgentOutputEnvelope` JSON object in the test with two directions named `Agent sharper hierarchy` and `Agent calmer rhythm`. Both directions must target the context package target id and include typed `setLayoutConstraints` operations with `baseRevision` equal to the context package source revision.
    - paste that JSON into `agent-output-json-input`.
    - click `ingest-agent-output-button`.
    - assert `variation-direction-list` contains `Agent sharper hierarchy` and `Agent calmer rhythm`.
    - assert `agent-output-diagnostics` contains `validated`.
    - promote `Agent sharper hierarchy`.
    - reload the page and assert the agent-generated direction and `agentRuns` persisted.
    - assert saved localStorage does not contain `iframe.contentDocument`.
    - assert no body horizontal overflow at 1440, 768, and 390 px widths.

    Add one rejection-path check in the same spec or a second test:

    - create a context package.
    - paste output with `sourceRevision: "rev_stale"`.
    - click `ingest-agent-output-button`.
    - assert `agent-output-diagnostics` contains `stale-revision`.
    - assert no new agent-generated direction is promoted or applied.
    - paste output with only one direction.
    - click `ingest-agent-output-button`.
    - assert `agent-output-diagnostics` contains `insufficient-directions` or `schema-error`.
    - paste output whose `runtime` differs from the runtime selected in the UI.
    - click `ingest-agent-output-button`.
    - assert `agent-output-diagnostics` contains `runtime-mismatch`.

    After implementation and all verification gates pass, update `.planning/REQUIREMENTS.md`:

    - mark `AI-01`, `AI-02`, `AI-03`, and `AI-04` as `[x]`.
    - update v2 traceability rows for `AI-01` through `AI-04` to `complete`.
    - keep wording honest: completion is local-first structured agent output ingestion, not hosted provider parity.

    Update `.planning/STATE.md`:

    - set `Phase 08 status` to `shipped` only after all gates pass.
    - update requirement coverage from `17/46` to `21/46`.
    - set next action to `/sunco:verify 8` or `/sunco:ship 8` according to the current SUNCO route after execution.

    Update `08-VERIFICATION.md` with the new evidence and keep the product boundary explicit: Phase 08 completes AI-01 through AI-04 through validated local-first agent output ingestion, while hosted provider adapters and richer live context remain later work.
  </action>
  <acceptance_criteria>
    - apps/web/tests/phase-08-agent-output.spec.ts exists
    - apps/web/tests/phase-08-agent-output.spec.ts contains "ingests agent generated selected-region directions"
    - apps/web/tests/phase-08-agent-output.spec.ts contains "Agent sharper hierarchy"
    - apps/web/tests/phase-08-agent-output.spec.ts contains "iframe.contentDocument"
    - apps/web/tests/phase-08-agent-output.spec.ts contains "stale-revision"
    - pnpm exec playwright test apps/web/tests/phase-08-agent-output.spec.ts exits 0
    - pnpm lint exits 0
    - pnpm typecheck exits 0
    - pnpm test exits 0
    - pnpm e2e exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] A selected canvas object can produce an `AgentContextPackage` containing target id, source revision, selected object summary, surrounding context, guardrails, and `docs/prompts/context-driven-design-agent-prompt.md`.
- [ ] Agent/model-shaped structured output can be ingested from JSON and stored as validated variation directions without using live iframe DOM as source of truth.
- [ ] Invalid agent output records are rejected or stored as rejected runs with specific diagnostics for stale revision, missing reference, out-of-scope target, unsafe patch, unsupported operation, insufficient directions, runtime mismatch, parse error, and schema error.
- [ ] Agent-generated directions appear side by side, can be promoted explicitly, survive reload, and replay only typed operations or typed patches.
- [ ] AI-01, AI-02, AI-03, and AI-04 are marked complete only after the implementation and verification evidence exists.
- [ ] All task acceptance criteria verified
- [ ] /sunco:lint passes with zero errors
- [ ] npx tsc --noEmit passes with zero errors
- [ ] pnpm lint passes with zero errors
- [ ] pnpm typecheck passes with zero errors
- [ ] pnpm test passes with zero failures
- [ ] pnpm e2e passes with zero failures
</done_when>
