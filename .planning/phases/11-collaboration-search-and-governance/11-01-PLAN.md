---
phase: 11
plan: 01
title: Build project home, organization, and smart search foundation
wave: 1
depends_on: []
files_modified:
  - packages/editor-core/src/schemas.ts
  - packages/editor-core/src/workspace-home.ts
  - packages/editor-core/src/integrity.ts
  - packages/editor-core/src/handoff.ts
  - packages/editor-core/src/index.ts
  - packages/editor-core/src/__tests__/workspace-home.test.ts
  - packages/editor-core/src/__tests__/persistence.test.ts
  - apps/web/components/project-home-panel.tsx
  - apps/web/components/editor-shell.tsx
  - apps/web/app/globals.css
  - apps/web/tests/phase-11-home-search.spec.ts
requirements_addressed:
  - HOME-01
  - HOME-02
  - HOME-03
gap_closure: false
runtime_impact: editor-core ProjectBundle metadata/search contracts plus web UI only; no hosted search service, auth service, external database, crawler, or marketplace is introduced
install_impact: none
docs_update: true
contracts:
  - Project home/search must derive from stored ProjectBundle and deterministic fixture/project records, not from live iframe DOM.
  - HOME-01 through HOME-03 are local-first workspace organization primitives; public marketplace and hosted account sync remain deferred.
  - Search must index title, folders, tags, source records, assets, design systems, artifact types, comments, notes, and parsed semantic text, not only hardcoded demo names.
smoke_commands:
  - pnpm lint
  - pnpm typecheck
  - npx tsc --noEmit
  - pnpm test
  - pnpm e2e
---

<objective>
Build the first-class project home and search foundation for Phase 11: recent work, examples, templates, design-system libraries, folders, tags, ownership, lifecycle status, and smart search over stored project/source/artifact content.

Requirements fulfilled: HOME-01, HOME-02, HOME-03
Phase goal contribution: This makes K-Design navigable like a professional design workspace without introducing hosted search or marketplace infrastructure. Complexity: L.
</objective>

<tasks>
<task id="11-01-01" type="auto">
  <name>Extend ProjectBundle with workspace organization metadata</name>
  <files>
    - packages/editor-core/src/schemas.ts
  </files>
  <read_first>
    - .planning/phases/11-collaboration-search-and-governance/11-CONTEXT.md
    - .planning/PRODUCT-NORTH-STAR.md
    - .planning/REQUIREMENTS.md
    - docs/guides/coding-principles.md
    - packages/editor-core/src/schemas.ts
  </read_first>
  <action>
    Add typed workspace/home schemas to `packages/editor-core/src/schemas.ts`.

    Required schemas and exported types:
    - `ProjectLifecycleStatusSchema = z.enum(["draft", "inReview", "approved", "archived"])`
    - `WorkspaceLibraryKindSchema = z.enum(["recent", "example", "template", "designSystemLibrary", "folder"])`
    - `WorkspaceProjectMetadataSchema` with `folderPath: string[]`, `tags: string[]`, `owner`, `status: ProjectLifecycleStatusSchema`, `libraryKind: WorkspaceLibraryKindSchema`, optional `description`, optional `thumbnailAssetId`, optional `sourceIds`, `createdAt`, `updatedAt`.
    - `WorkspaceFolderSchema` with `id`, optional `parentId`, `name`, `path: string[]`, `createdAt`, `updatedAt`.
    - `WorkspaceSearchFieldSchema = z.enum(["title", "tag", "folder", "owner", "status", "source", "asset", "designSystem", "artifactType", "semantic"])`
    - `WorkspaceSearchHitSchema` with `id`, `projectId`, `field: WorkspaceSearchFieldSchema`, `label`, `value`, `score`, optional `sourceId`, optional `assetId`, optional `objectId`, optional `nodeId`.
    - `WorkspaceSearchIndexSchema` with `projectId`, `revision`, `hits: WorkspaceSearchHitSchema[]`, `updatedAt`.

    Extend `ProjectBundleSchema` with:
    - `workspace: WorkspaceProjectMetadataSchema.optional()`
    - `workspaceFolders: z.array(WorkspaceFolderSchema).default([])`
    - `workspaceSearchIndex: WorkspaceSearchIndexSchema.optional()`

    Preserve all existing Phase 01-10 fields and do not change `schemaVersion`.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/schemas.ts contains "export const WorkspaceProjectMetadataSchema"
    - packages/editor-core/src/schemas.ts contains "export const WorkspaceSearchIndexSchema"
    - packages/editor-core/src/schemas.ts contains "workspace: WorkspaceProjectMetadataSchema.optional()"
    - packages/editor-core/src/schemas.ts contains "workspaceFolders: z.array(WorkspaceFolderSchema).default([])"
  </acceptance_criteria>
</task>

<task id="11-01-02" type="auto">
  <name>Create deterministic workspace home and search helpers</name>
  <files>
    - packages/editor-core/src/workspace-home.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/workspace-home.test.ts
  </files>
  <read_first>
    - .planning/phases/11-collaboration-search-and-governance/11-CONTEXT.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/ids.ts
    - packages/editor-core/src/context-ingestion.ts
    - packages/editor-core/src/design-system.ts
    - packages/editor-core/src/slides.ts
    - packages/editor-core/src/index.ts
  </read_first>
  <action>
    Create `packages/editor-core/src/workspace-home.ts`. Export:
    - `createWorkspaceMetadata(bundle, input)`
    - `appendWorkspaceFolder(bundle, input)`
    - `moveProjectToFolder(bundle, folderId, createdAt?)`
    - `setProjectTags(bundle, tags, createdAt?)`
    - `setProjectLifecycleStatus(bundle, status, createdAt?)`
    - `createWorkspaceSearchIndex(bundle, createdAt?)`
    - `searchWorkspaceBundles(bundles, query)`
    - `createWorkspaceHomeState(bundles, createdAt?)`

    Behavior requirements:
    - Use `stableHash` for deterministic folder/index ids.
    - Folder paths must be normalized: trim empty segments, reject `..`, reject `/`, and reject duplicate sibling folder paths.
    - Tags must be normalized to lowercase trimmed strings, deduped, and capped to 32 tags per project.
    - Search index must include project title, workspace tags/folders/owner/status, source record names/urls/kinds, asset ids/source urls/licenses, design system names/tokens/code references, slide deck titles, export artifact kinds, generated notes, parsed context `summary`/`textBlocks`/`frameNames`, comments, and ready-for-dev labels.
    - `searchWorkspaceBundles` must be deterministic and score exact title/tag/folder hits above fuzzy semantic hits.
    - No remote fetch, crawler, hosted account, marketplace listing, or live iframe DOM query is allowed.

    Add `export * from "./workspace-home.js";` to `packages/editor-core/src/index.ts`.

    Add `workspace-home.test.ts` covering metadata creation, folder nesting, invalid folder rejection, tag normalization, lifecycle status changes, search over title/tag/source/design-system/artifact type/semantic text, deterministic ordering, empty query handling, and reload schema roundtrip through `ProjectBundleSchema`.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/workspace-home.ts exists
    - packages/editor-core/src/workspace-home.ts contains "export function createWorkspaceSearchIndex("
    - packages/editor-core/src/workspace-home.ts contains "export function searchWorkspaceBundles("
    - packages/editor-core/src/workspace-home.ts contains "export function createWorkspaceHomeState("
    - packages/editor-core/src/index.ts contains "export * from \"./workspace-home.js\""
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/workspace-home.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="11-01-03" type="auto">
  <name>Validate persisted workspace records and include them in handoff</name>
  <files>
    - packages/editor-core/src/integrity.ts
    - packages/editor-core/src/handoff.ts
    - packages/editor-core/src/__tests__/persistence.test.ts
  </files>
  <read_first>
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/workspace-home.ts
    - packages/editor-core/src/integrity.ts
    - packages/editor-core/src/handoff.ts
    - packages/editor-core/src/__tests__/persistence.test.ts
  </read_first>
  <action>
    Update persisted integrity so invalid workspace state is rejected on load:
    - `workspace.thumbnailAssetId` must reference an existing asset.
    - `workspace.sourceIds` must reference existing source records.
    - Workspace folder ids must be unique.
    - A folder cannot parent itself or create a parent cycle.
    - `workspace.folderPath` must match an existing folder when the folder list is non-empty.
    - `workspaceSearchIndex.projectId` must match `bundle.id`.
    - `workspaceSearchIndex.revision` must match `bundle.baseRevision`.
    - Each search hit `projectId` must match `bundle.id`.
    - Search hit `sourceId`, `assetId`, `objectId`, and `nodeId` references must exist when present.

    Add `workspace`, `workspaceFolders`, and `workspaceSearchIndex` to `createHandoff(...).includes` in `packages/editor-core/src/handoff.ts`.

    Add persistence regression tests for missing thumbnail assets, missing source ids, folder cycles, stale index revisions, and search hits referencing missing objects/nodes/assets/sources.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/integrity.ts contains "workspaceSearchIndex"
    - packages/editor-core/src/handoff.ts contains "\"workspace\""
    - packages/editor-core/src/handoff.ts contains "\"workspaceSearchIndex\""
    - packages/editor-core/src/__tests__/persistence.test.ts contains "workspace"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/workspace-home.test.ts src/__tests__/persistence.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="11-01-04" type="auto">
  <name>Add project home and smart search UI</name>
  <files>
    - apps/web/components/project-home-panel.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
    - apps/web/tests/phase-11-home-search.spec.ts
  </files>
  <read_first>
    - apps/web/components/editor-shell.tsx
    - apps/web/components/dev-mode-panel.tsx
    - apps/web/components/export-publish-panel.tsx
    - apps/web/app/globals.css
    - apps/web/tests/phase-10-dev-mode.spec.ts
    - packages/editor-core/src/workspace-home.ts
  </read_first>
  <action>
    Add `apps/web/components/project-home-panel.tsx` and wire it into the studio shell.

    UI requirements:
    - A first-class project home surface must expose tabs or grouped sections for Recent, Your designs, Examples, Templates, Design systems, and Folders.
    - The panel must show folder path, tags, owner, lifecycle status, source count, artifact/export type count, and updated time for the current project and deterministic example/template records.
    - Search input must run `searchWorkspaceBundles` and display matches grouped by field: title, tag, source, design system, artifact type, semantic content.
    - Folder/tag/status actions must write typed workspace metadata through editor-core helpers and survive reload.
    - Empty state must not look like a marketing landing page; it should be a usable product workspace.
    - The layout must have no horizontal overflow at desktop, 768px tablet, and mobile widths.

    Add Playwright coverage:
    - Project home opens and shows Recent, Examples, Templates, Design systems, and Folders.
    - Search finds the current project by title, tag, source asset/name, design system, artifact type, and semantic text.
    - Folder/tag/status changes persist after reload.
    - Tablet/mobile widths do not overflow horizontally.
  </action>
  <acceptance_criteria>
    - apps/web/components/project-home-panel.tsx exists
    - apps/web/components/project-home-panel.tsx contains "Recent"
    - apps/web/components/project-home-panel.tsx contains "Templates"
    - apps/web/components/project-home-panel.tsx contains "Design systems"
    - apps/web/components/editor-shell.tsx imports "ProjectHomePanel"
    - apps/web/tests/phase-11-home-search.spec.ts contains "search"
    - pnpm e2e -- tests/phase-11-home-search.spec.ts exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] HOME-01, HOME-02, and HOME-03 have concrete schemas, helpers, persisted integrity, handoff inclusion, UI, reload proof, and E2E coverage.
- [ ] Search indexes title, tags, folders, source records, assets, design systems, artifact types, comments/notes, and semantic context text.
- [ ] Folder/tag/lifecycle actions persist through `ProjectBundleSchema` reload.
- [ ] Browser tests pass at desktop, tablet, and mobile without horizontal overflow.
- [ ] `pnpm lint` exits 0.
- [ ] `pnpm typecheck` exits 0.
- [ ] `npx tsc --noEmit` exits 0.
- [ ] `pnpm test` exits 0.
- [ ] `pnpm e2e` exits 0.
</done_when>
