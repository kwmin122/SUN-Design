---
phase: 7
plan: 02
title: Design-System Product Workflow and Component Playground
wave: 2
depends_on: [07-01]
files_modified:
  - apps/web/components/design-system-panel.tsx
  - apps/web/components/component-playground-panel.tsx
  - apps/web/components/editor-shell.tsx
  - apps/web/app/globals.css
  - apps/web/tests/phase-07-design-system.spec.ts
  - apps/web/tests/phase-07-playground.spec.ts
requirements_addressed:
  - DSGN-04
  - DSGN-05
  - DSGN-06
  - DSGN-07
  - DSGN-08
gap_closure: false
---

<objective>
Turn the Phase 07 core model into a visible product workflow: review extracted design-system candidates, approve/reject tokens and component patterns, map tokens to code, add Storybook/GitHub/docs/code references, publish/remix/rollback a design-system version, and test component variants/props in a non-mutating playground.

Complexity: L
Requirements fulfilled: DSGN-04, DSGN-05, DSGN-06, DSGN-07, DSGN-08
Phase goal contribution: This makes design systems concrete and auditable in the studio UI instead of leaving Phase 07 as hidden data-model work.
</objective>

<tasks>
<task id="7-02-01" type="auto">
  <name>Create governed design-system review and publish panel</name>
  <files>
    - apps/web/components/design-system-panel.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </files>
  <read_first>
    - .planning/phases/07-design-system-tokens-and-code-connect/07-CONTEXT.md
    - .planning/phases/07-design-system-tokens-and-code-connect/07-RESEARCH.md
    - .planning/phases/07-design-system-tokens-and-code-connect/07-01-PLAN.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/design-system.ts
    - packages/editor-core/src/handoff.ts
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
    - apps/web/tests/phase-05.spec.ts
  </read_first>
  <action>
    Create `apps/web/components/design-system-panel.tsx` as a client component with props:
    - `bundle: ProjectBundle`
    - `onExtractCandidates(): void`
    - `onApproveItems(itemIds: string[]): void`
    - `onRejectItems(itemIds: string[]): void`
    - `onMapToken(tokenId: string, mapping: { cssVariable?: string; tailwindClass?: string; codeReferenceId?: string }): void`
    - `onAddCodeReference(input: { name: string; framework: string; importPath: string; exportName: string; sourcePath?: string; sourceUrl?: string; docsUrl?: string; storybookUrl?: string }): void`
    - `onPublish(label: string): void`
    - `onRemix(name: string): void`
    - `onRollback(versionId: string): void`

    Render root `data-testid="design-system-panel"` and include:
    - button `Extract candidates`
    - token list `data-testid="design-token-list"`
    - component pattern list `data-testid="component-pattern-list"`
    - code reference list `data-testid="code-reference-list"`
    - version list `data-testid="design-system-version-list"`
    - buttons `Approve selected token`, `Reject selected token`, `Publish system`, `Remix system`, and `Rollback system`
    - inputs `token-css-variable-input`, `token-tailwind-class-input`, `code-ref-name-input`, `code-ref-framework-input`, `code-ref-import-input`, `code-ref-export-input`, `code-ref-source-input`, `code-ref-docs-input`, `code-ref-storybook-input`, `publish-label-input`, and `remix-name-input`

    Keep the UI dense and tool-like. Avoid landing-page copy and do not add decorative marketing sections.

    Update `apps/web/components/editor-shell.tsx`:
    - import Phase 07 helpers from `@kdesign/editor-core`.
    - replace or extend the old one-click `Design System & Handoff` block so the new panel is visible in the right rail.
    - `Extract candidates` must call `extractDesignSystemCandidates(projectBundle)`.
    - approve/reject/map/add-code-ref/publish/remix/rollback callbacks must commit a parsed `ProjectBundle` with `designSystem` updated and undo tracking enabled.
    - existing Phase 05 share/handoff controls must remain available.

    Add CSS classes `.design-system-panel`, `.token-row`, `.token-row.selected`, `.code-ref-grid`, `.version-row`, `.system-action-bar`, and responsive wrapping rules. The panel must not create body horizontal overflow at 1440, 768, or 390 px widths.
  </action>
  <acceptance_criteria>
    - apps/web/components/design-system-panel.tsx exists
    - apps/web/components/design-system-panel.tsx contains "data-testid=\"design-system-panel\""
    - apps/web/components/design-system-panel.tsx contains "Extract candidates"
    - apps/web/components/design-system-panel.tsx contains "token-css-variable-input"
    - apps/web/components/editor-shell.tsx contains "<DesignSystemPanel"
    - apps/web/app/globals.css contains ".design-system-panel"
    - pnpm --filter @kdesign/web typecheck exits 0
  </acceptance_criteria>
</task>

<task id="7-02-02" type="auto">
  <name>Add non-mutating component playground panel</name>
  <files>
    - apps/web/components/component-playground-panel.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </files>
  <read_first>
    - .planning/phases/07-design-system-tokens-and-code-connect/07-CONTEXT.md
    - .planning/phases/07-design-system-tokens-and-code-connect/07-RESEARCH.md
    - .planning/phases/07-design-system-tokens-and-code-connect/07-01-PLAN.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/design-system.ts
    - packages/editor-core/src/canvas-graph.ts
    - apps/web/components/component-instance-panel.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </read_first>
  <action>
    Create `apps/web/components/component-playground-panel.tsx` with props:
    - `graph: CanvasGraph | undefined`
    - `designSystem: DesignSystem | undefined`
    - `onCreatePlaygroundState(componentId: string, input: { variantId?: string; propValues?: Record<string, unknown>; mode?: string }): void`
    - `playgroundState`

    Render root `data-testid="component-playground-panel"`.
    Requirements:
    - show available local components and their variants from `graph.components`.
    - include select/input controls with test ids `playground-component-select`, `playground-variant-select`, `playground-prop-key-input`, `playground-prop-value-input`, and `playground-mode-input`.
    - button `Preview playground state`.
    - render a summary `data-testid="playground-state-summary"` showing component name, variant name, mode, prop keys, and snapshot hash.
    - never call `commitCanvasOperation`, never append `canvasOperations`, and never mutate `graph`.
    - when there are no components, show a compact empty state that points to the existing component panel.

    Update `apps/web/components/editor-shell.tsx` to hold local `playgroundState` React state and render `ComponentPlaygroundPanel` below the component instance panel or design-system panel in the right rail.

    Add CSS classes `.component-playground-panel`, `.playground-preview`, and `.playground-controls`.
  </action>
  <acceptance_criteria>
    - apps/web/components/component-playground-panel.tsx exists
    - apps/web/components/component-playground-panel.tsx contains "data-testid=\"component-playground-panel\""
    - apps/web/components/component-playground-panel.tsx contains "Preview playground state"
    - apps/web/components/editor-shell.tsx contains "<ComponentPlaygroundPanel"
    - apps/web/app/globals.css contains ".component-playground-panel"
    - pnpm --filter @kdesign/web typecheck exits 0
  </acceptance_criteria>
</task>

<task id="7-02-03" type="auto">
  <name>Add browser workflow tests for governed design systems and playground</name>
  <files>
    - apps/web/tests/phase-07-design-system.spec.ts
    - apps/web/tests/phase-07-playground.spec.ts
    - apps/web/tests/phase-05.spec.ts
    - apps/web/tests/phase-06-components.spec.ts
  </files>
  <read_first>
    - .planning/phases/07-design-system-tokens-and-code-connect/07-CONTEXT.md
    - .planning/phases/07-design-system-tokens-and-code-connect/07-RESEARCH.md
    - .planning/phases/07-design-system-tokens-and-code-connect/07-01-PLAN.md
    - .planning/phases/07-design-system-tokens-and-code-connect/07-02-PLAN.md
    - apps/web/tests/phase-05.spec.ts
    - apps/web/tests/phase-06-components.spec.ts
    - apps/web/tests/phase-06-canvas.spec.ts
    - playwright.config.ts
  </read_first>
  <action>
    Create `apps/web/tests/phase-07-design-system.spec.ts`:
    - open a fresh project and wait for readiness.
    - click `Extract candidates`.
    - assert `design-token-list` contains at least one `color.` token and a typography token.
    - select/approve a token with `Approve selected token`.
    - map it with CSS variable `--brand-primary` and Tailwind class `text-brand-primary`.
    - add a code reference named `MarketingCard` with framework `react`, import path `@/components/marketing-card`, export name `MarketingCard`, source path `apps/web/components/marketing-card.tsx`, docs URL `https://example.com/docs/marketing-card`, and Storybook URL `https://example.com/storybook/marketing-card`.
    - publish with label `Studio System v1`.
    - remix with name `Studio System Remix`.
    - rollback to the published version.
    - reload and assert localStorage plus UI still contain `Studio System v1`, `--brand-primary`, `text-brand-primary`, `MarketingCard`, `designTokens`, and `codeReferences`.
    - assert body width has no horizontal overflow at 1440, 768, and 390 px.

    Create `apps/web/tests/phase-07-playground.spec.ts`:
    - create a local component using the existing Phase 06 component workflow.
    - capture the `canvasOperations.length` from localStorage.
    - use the new playground controls to preview a variant, prop value, and mode.
    - assert `playground-state-summary` updates with the component name, variant, mode, prop key, and hash.
    - assert after preview that `canvasOperations.length` is unchanged and the selected canvas object/instance state is unchanged.
    - reload and assert the committed canvas component still exists while playground state remains non-persistent UI state unless intentionally re-created.

    Update existing Phase 05/06 tests only if selectors need to be scoped after adding the richer panels. Do not weaken their assertions.
  </action>
  <acceptance_criteria>
    - apps/web/tests/phase-07-design-system.spec.ts exists
    - apps/web/tests/phase-07-design-system.spec.ts contains "Studio System v1"
    - apps/web/tests/phase-07-design-system.spec.ts contains "--brand-primary"
    - apps/web/tests/phase-07-design-system.spec.ts contains "MarketingCard"
    - apps/web/tests/phase-07-playground.spec.ts exists
    - apps/web/tests/phase-07-playground.spec.ts contains "canvasOperations.length"
    - apps/web/tests/phase-07-playground.spec.ts contains "playground-state-summary"
    - pnpm e2e -- apps/web/tests/phase-07-design-system.spec.ts apps/web/tests/phase-07-playground.spec.ts exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] The right rail exposes a real design-system review panel, not only the old one-click learning button.
- [ ] User can extract candidates, approve/reject tokens or patterns, map a token to CSS variable and Tailwind class, and add a code component reference.
- [ ] User can publish, remix, roll back to an actual stored version snapshot, reload, and still see design-system versions and mappings.
- [ ] Component playground can preview local component variants, props, and modes without mutating the main canvas graph or operation log.
- [ ] Share and agent-handoff controls from Phase 05 still work after the richer design-system UI is added.
- [ ] Desktop, tablet, and mobile browser checks have no horizontal body overflow.
- [ ] Phase 08-11-only features such as AI variation generation, real context ingestion, hosted publish, multiplayer collaboration, and live data binding are not implemented in this phase.
- [ ] All task acceptance criteria verified
- [ ] /sunco:lint passes with zero errors
- [ ] npx tsc --noEmit passes with zero errors
- [ ] pnpm lint exits 0
- [ ] pnpm typecheck exits 0
- [ ] pnpm e2e -- apps/web/tests/phase-07-design-system.spec.ts apps/web/tests/phase-07-playground.spec.ts exits 0
</done_when>
