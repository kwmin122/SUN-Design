---
phase: 7
plan: 01
title: Core Design-System Governance and Code Mapping
wave: 1
depends_on: []
files_modified:
  - packages/editor-core/src/schemas.ts
  - packages/editor-core/src/design-system.ts
  - packages/editor-core/src/handoff.ts
  - packages/editor-core/src/index.ts
  - packages/editor-core/src/__tests__/schemas.test.ts
  - packages/editor-core/src/__tests__/design-system.test.ts
  - packages/editor-core/src/__tests__/handoff.test.ts
requirements_addressed:
  - DSGN-04
  - DSGN-05
  - DSGN-06
  - DSGN-07
  - DSGN-08
gap_closure: false
---

<objective>
Promote the current design-system placeholder into a governed, versioned, code-connected core model. The implementation must preserve old bundles while adding reviewable design-system candidates, stable tokens, code references, component patterns, publish/remix/rollback, and non-mutating component playground state.

Complexity: L
Requirements fulfilled: DSGN-04, DSGN-05, DSGN-06, DSGN-07, DSGN-08
Phase goal contribution: This gives Phase 07 a real data contract comparable in direction to Claude Design design-system setup, Paper tokens/code components, and Figma Dev Mode/Code Connect without provider lock-in or external API dependency.
</objective>

<tasks>
<task id="7-01-01" type="auto">
  <name>Extend design-system schemas with tokens, code refs, patterns, and versions</name>
  <files>
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/__tests__/schemas.test.ts
  </files>
  <read_first>
    - CLAUDE.md
    - .planning/PRODUCT-NORTH-STAR.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/07-design-system-tokens-and-code-connect/07-CONTEXT.md
    - .planning/phases/07-design-system-tokens-and-code-connect/07-RESEARCH.md
    - .planning/research/COMPETITIVE-GAP-REVIEW.md
    - docs/guides/coding-principles.md
    - docs/prompts/context-driven-design-agent-prompt.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/__tests__/schemas.test.ts
  </read_first>
  <action>
    Extend `packages/editor-core/src/schemas.ts` without breaking existing serialized bundles.

    Add exported schemas and types:
    - `DesignTokenCategorySchema = z.enum(["color", "typography", "spacing", "radius", "shadow", "layout", "component", "motion", "other"])`
    - `DesignSystemItemStatusSchema = z.enum(["candidate", "approved", "rejected", "published"])`
    - `DesignTokenModeSchema` with `mode: string` and `value: string`
    - `DesignTokenCodeMappingSchema` with optional `cssVariable`, `tailwindClass`, and `codeReferenceId`
    - `DesignTokenSchema` with `id`, `name`, `category`, `value`, optional `modes`, optional `description`, `provenance`, `status`, and optional `codeMapping`
    - `CodeComponentReferenceSchema` with `id`, `name`, `framework`, `importPath`, `exportName`, optional `sourcePath`, optional `sourceUrl`, optional `docsUrl`, optional `storybookUrl`, `propMappings`, `slotMappings`, and `status`
    - `DesignComponentPatternSchema` with `id`, `name`, optional `sourceObjectId`, optional `componentId`, `variantIds`, `propNames`, `tokenIds`, optional `codeReferenceId`, `provenance`, and `status`
    - `DesignSystemVersionSnapshotSchema` with `colors`, `typography`, `radius`, `spacing`, `tokens`, `codeReferences`, `componentPatterns`, and `publishState`
    - `DesignSystemVersionSchema` with `id`, `label`, `sourceRevision`, `tokenCount`, `componentPatternCount`, `snapshotHash`, `snapshot`, and `createdAt`
    - `DesignSystemPublishStateSchema = z.enum(["draft", "reviewed", "published"])`

    Extend `DesignSystemSchema` with optional/defaulted fields:
    - `tokens: z.array(DesignTokenSchema).default([])`
    - `codeReferences: z.array(CodeComponentReferenceSchema).default([])`
    - `componentPatterns: z.array(DesignComponentPatternSchema).default([])`
    - `versions: z.array(DesignSystemVersionSchema).default([])`
    - `publishState: DesignSystemPublishStateSchema.default("draft")`
    - `updatedAt: z.string().min(1).optional()`

    Preserve existing required fields `colors`, `typography`, `radius`, `spacing`, `source`, and `createdAt` so Phase 05 bundles remain valid.

    Update schema tests:
    - existing old-shape `designSystem` fixtures must still parse and default the new arrays.
    - a full design system with tokens, code references, component patterns, versions, and `publishState` must parse.
    - invalid token category or invalid publish state must fail.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/schemas.ts contains "export const DesignTokenSchema"
    - packages/editor-core/src/schemas.ts contains "export const CodeComponentReferenceSchema"
    - packages/editor-core/src/schemas.ts contains "export const DesignComponentPatternSchema"
    - packages/editor-core/src/schemas.ts contains "export const DesignSystemVersionSchema"
    - packages/editor-core/src/schemas.ts contains "tokens: z.array(DesignTokenSchema).default([])"
    - packages/editor-core/src/__tests__/schemas.test.ts contains "parses legacy design systems with governance defaults"
    - packages/editor-core/src/__tests__/schemas.test.ts contains "accepts governed design-system records"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/schemas.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="7-01-02" type="auto">
  <name>Add deterministic design-system extraction, review, publish, remix, and rollback helpers</name>
  <files>
    - packages/editor-core/src/design-system.ts
    - packages/editor-core/src/handoff.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/design-system.test.ts
    - packages/editor-core/src/__tests__/handoff.test.ts
  </files>
  <read_first>
    - .planning/phases/07-design-system-tokens-and-code-connect/07-CONTEXT.md
    - .planning/phases/07-design-system-tokens-and-code-connect/07-RESEARCH.md
    - .planning/phases/06-canvas-and-component-model/06-CONTEXT.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/handoff.ts
    - packages/editor-core/src/canvas-graph.ts
    - packages/editor-core/src/canvas-components.ts
    - packages/editor-core/src/ids.ts
    - packages/editor-core/src/__tests__/handoff.test.ts
  </read_first>
  <action>
    Create `packages/editor-core/src/design-system.ts` with deterministic exported helpers:
    - `extractDesignSystemCandidates(bundle: ProjectBundle, createdAt?: string): DesignSystem`
    - `approveDesignSystemItems(system: DesignSystem, itemIds: string[]): DesignSystem`
    - `rejectDesignSystemItems(system: DesignSystem, itemIds: string[]): DesignSystem`
    - `publishDesignSystem(system: DesignSystem, input: { label: string; sourceRevision: string; createdAt?: string }): DesignSystem`
    - `remixDesignSystem(system: DesignSystem, input: { name?: string; tokenUpdates?: Record<string, string>; createdAt?: string }): DesignSystem`
    - `rollbackDesignSystem(system: DesignSystem, versionId: string): DesignSystem`
    - `snapshotDesignSystem(system: DesignSystem): string`

    `extractDesignSystemCandidates` must:
    - preserve compatibility with the previous `learnDesignSystem` output shape.
    - extract hex colors from `bundle.html.normalized` into `color.*` token names.
    - create typography, radius, and spacing tokens from the current design system fallback values.
    - create component pattern candidates from `bundle.canvasGraph.components` when present.
    - mark extracted tokens/patterns as `candidate` with provenance strings.
    - dedupe token names deterministically and reject duplicate token names after normalization.

    `publishDesignSystem` must:
    - require at least one approved or published token.
    - set `publishState: "published"`.
    - append a `DesignSystemVersion` with deterministic `snapshotHash`, `tokenCount`, and `componentPatternCount`.
    - convert approved tokens/patterns to `published`.

    `rollbackDesignSystem` must reject missing version ids and restore the selected version's stored snapshot for colors, typography, radius, spacing, tokens, code references, component patterns, and publish state while preserving the full version history.

    Keep `learnDesignSystem` in `packages/editor-core/src/handoff.ts` working by delegating to `extractDesignSystemCandidates` or by enriching its returned object with the new schema fields. Existing Phase 05 tests must still pass.

    Update `packages/editor-core/src/index.ts` to export `./design-system.js`.

    Update handoff helpers so generated handoff packages include `"designTokens"`, `"codeReferences"`, `"componentPatterns"`, and `"designSystemVersions"` in addition to the existing `"designSystem"` include.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/design-system.ts exists
    - packages/editor-core/src/design-system.ts contains "export function extractDesignSystemCandidates("
    - packages/editor-core/src/design-system.ts contains "export function publishDesignSystem("
    - packages/editor-core/src/design-system.ts contains "export function rollbackDesignSystem("
    - packages/editor-core/src/handoff.ts contains "designTokens"
    - packages/editor-core/src/index.ts contains "export * from \"./design-system.js\""
    - packages/editor-core/src/__tests__/design-system.test.ts contains "extracts reviewable design-system candidates"
    - packages/editor-core/src/__tests__/design-system.test.ts contains "publishes remixes and rolls back design-system versions"
    - packages/editor-core/src/__tests__/handoff.test.ts contains "designTokens"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/design-system.test.ts src/__tests__/handoff.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="7-01-03" type="auto">
  <name>Add token code mapping, code-reference validation, and non-mutating playground state</name>
  <files>
    - packages/editor-core/src/design-system.ts
    - packages/editor-core/src/__tests__/design-system.test.ts
  </files>
  <read_first>
    - .planning/phases/07-design-system-tokens-and-code-connect/07-CONTEXT.md
    - .planning/phases/07-design-system-tokens-and-code-connect/07-RESEARCH.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/design-system.ts
    - packages/editor-core/src/canvas-graph.ts
    - packages/editor-core/src/canvas-components.ts
    - packages/editor-core/src/__tests__/canvas-components.test.ts
    - packages/editor-core/src/__tests__/canvas-graph.test.ts
  </read_first>
  <action>
    Add these exports to `packages/editor-core/src/design-system.ts`:
    - `mapTokenToCode(system: DesignSystem, tokenId: string, mapping: { cssVariable?: string; tailwindClass?: string; codeReferenceId?: string }): DesignSystem`
    - `addCodeComponentReference(system: DesignSystem, reference: Omit<CodeComponentReference, "id" | "status"> & { id?: string; status?: CodeComponentReference["status"] }): DesignSystem`
    - `validateCodeComponentReference(reference: CodeComponentReference): void`
    - `createComponentPlaygroundState(input: { graph: CanvasGraph; componentId: string; variantId?: string; propValues?: Record<string, unknown>; mode?: string }): { componentId: string; componentName: string; variantId: string | undefined; variantName: string; propValues: Record<string, unknown>; mode: string; snapshotHash: string }`

    Validation requirements:
    - CSS variables must match `^--[a-z0-9][a-z0-9-]*$`.
    - Tailwind classes must be non-empty and must not contain `<`, `>`, `"`, `'`, `;`, `javascript:`, or `url(`.
    - `sourcePath` must be repo-relative, must not start with `/`, must not include `..`, and must not include `://`.
    - `sourceUrl`, `docsUrl`, and `storybookUrl`, when present, must be `https://` URLs.
    - `mapTokenToCode` must reject unknown token ids and unknown code reference ids.
    - `addCodeComponentReference` must reject duplicate reference names and invalid paths/URLs.
    - `createComponentPlaygroundState` must not mutate `graph`; tests must deep-clone before/after and assert equality.

    Unit tests must cover valid CSS variable mapping, Tailwind mapping, code reference add, unknown references, unsafe paths, unsafe URLs, duplicate code references, and playground non-mutation.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/design-system.ts contains "export function mapTokenToCode("
    - packages/editor-core/src/design-system.ts contains "export function addCodeComponentReference("
    - packages/editor-core/src/design-system.ts contains "export function createComponentPlaygroundState("
    - packages/editor-core/src/__tests__/design-system.test.ts contains "rejects unsafe code component references"
    - packages/editor-core/src/__tests__/design-system.test.ts contains "maps tokens to CSS variables and Tailwind classes"
    - packages/editor-core/src/__tests__/design-system.test.ts contains "creates component playground state without mutating the graph"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/design-system.test.ts exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] Legacy `designSystem` records still parse with governed defaults.
- [ ] Design-system candidates include deterministic tokens and component patterns from stored bundle state.
- [ ] Tokens support category, value, modes, provenance, status, CSS variable mapping, Tailwind mapping, and optional code-reference mapping.
- [ ] Code component references validate framework/import/export/source/docs/storybook metadata without network access.
- [ ] Design systems can approve/reject candidates, publish versions with snapshots, remix drafts, and restore a prior published version snapshot.
- [ ] Component playground state can be derived without mutating `CanvasGraph`.
- [ ] Agent handoff packages include design tokens, code references, component patterns, and design-system versions.
- [ ] Invalid token mappings, duplicate token/reference names, unsafe source paths, unsafe URLs, missing versions, and unknown token/code refs are rejected by tests.
- [ ] All task acceptance criteria verified
- [ ] /sunco:lint passes with zero errors
- [ ] npx tsc --noEmit passes with zero errors
- [ ] pnpm lint exits 0
- [ ] pnpm typecheck exits 0
- [ ] pnpm --filter @kdesign/editor-core test -- src/__tests__/schemas.test.ts src/__tests__/design-system.test.ts src/__tests__/handoff.test.ts exits 0
</done_when>
