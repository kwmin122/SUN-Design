---
phase: 9
plan: 01
title: Build context, data, asset, and sync core foundation
wave: 1
depends_on: []
files_modified:
  - packages/editor-core/src/schemas.ts
  - packages/editor-core/src/context-ingestion.ts
  - packages/editor-core/src/data-bindings.ts
  - packages/editor-core/src/asset-lifecycle.ts
  - packages/editor-core/src/sync.ts
  - packages/editor-core/src/integrity.ts
  - packages/editor-core/src/persistence.ts
  - packages/editor-core/src/export.ts
  - packages/editor-core/src/handoff.ts
  - packages/editor-core/src/index.ts
  - packages/editor-core/src/__tests__/context-ingestion.test.ts
  - packages/editor-core/src/__tests__/data-bindings.test.ts
  - packages/editor-core/src/__tests__/asset-lifecycle.test.ts
  - packages/editor-core/src/__tests__/sync.test.ts
  - packages/editor-core/src/__tests__/persistence.test.ts
requirements_addressed:
  - CTX-01
  - CTX-02
  - CTX-03
  - DATA-01
  - DATA-02
  - ASSET-01
gap_closure: false
---

<objective>
Build the editor-core foundation for Phase 09: first-class source ingestion records, generated source/design notes, safe web snapshots, data sources and bindings, asset lifecycle records, stable project asset URLs, and a server-portable sync envelope.

Requirements fulfilled: CTX-01, CTX-02, CTX-03, DATA-01 foundation-only, DATA-02, ASSET-01
Phase goal contribution: This plan replaces placeholder context metadata with validated stored state while preserving the ProjectBundle source-of-truth contract. Complexity: L.
</objective>

<tasks>
<task id="9-01-01" type="auto">
  <name>Extend ProjectBundle context schemas</name>
  <files>
    - packages/editor-core/src/schemas.ts
  </files>
  <read_first>
    - .planning/phases/09-context-ingestion-live-data-and-assets/09-CONTEXT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/PRODUCT-NORTH-STAR.md
    - docs/prompts/context-driven-design-agent-prompt.md
    - docs/guides/coding-principles.md
    - packages/editor-core/src/schemas.ts
  </read_first>
  <action>
    Update `packages/editor-core/src/schemas.ts` with versioned Phase 09 schemas and exported types.

    Add these enums:
    - `SourceKindSchema = z.enum(["image", "screenshot", "url", "webCapture", "document", "slideDeck", "spreadsheet", "csv", "json", "apiFixture", "figma", "codebase"])`
    - `SourceParseStatusSchema = z.enum(["queued", "parsed", "partial", "failed", "blocked"])`
    - `SourceUsageStatusSchema = z.enum(["candidate", "approved", "used", "rejected", "unknownRights", "blocked"])`
    - `GeneratedNoteKindSchema = z.enum(["source-notes", "design-context"])`
    - `WebSnapshotStatusSchema = z.enum(["editable", "referenceOnly", "blocked"])`
    - `DataSourceKindSchema = z.enum(["csv", "spreadsheet", "staticJson", "apiFixture"])`
    - `DataBindingStateSchema = z.enum(["ready", "empty", "loading", "error"])`
    - `AssetLifecycleEventTypeSchema = z.enum(["candidate", "verified", "cached", "relinked", "replaced", "blocked", "placeholder"])`
    - `SyncStatusSchema = z.enum(["localOnly", "synced", "diverged", "rejected"])`

    Add these object schemas and exported types:
    - `SourceRecordSchema` with fields `id`, `kind`, `name`, `hash`, `createdAt`, `importedAt`, optional `sourceUrl`, optional `localPath`, optional `mimeType`, optional `assetIds`, `parseStatus`, `usageStatus`, optional `rights`, optional `uncertainty`, and `diagnostics`.
    - `IngestionJobSchema` with fields `id`, `sourceId`, `status: SourceParseStatusSchema`, `createdAt`, `updatedAt`, and `diagnostics`.
    - `GeneratedNoteSchema` with fields `id`, `kind`, `path`, `sourceIds`, `content`, `createdAt`, `updatedAt`.
    - `WebSnapshotSchema` with fields `id`, `sourceId`, `url`, `status`, optional `sanitizedHtml`, optional `normalizedHtml`, optional `screenshotAssetId`, `canvasObjectIds`, `diagnostics`, and `createdAt`.
    - `DataSourceSchema` with fields `id`, `kind`, `name`, `sourceId`, `fields`, `rows`, `status`, `createdAt`, `updatedAt`.
    - `DataBindingSchema` with fields `id`, `dataSourceId`, `targetObjectId`, optional `targetNodeId`, `fieldMap`, `rowLimit`, `state`, `sourceRevision`, `createdAt`, `updatedAt`.
    - `AssetLifecycleEventSchema` with fields `id`, `assetId`, `type`, optional `sourceId`, optional `previousAssetId`, optional `nextAssetId`, optional `reason`, `createdAt`.
    - `ProjectAssetUrlSchema` with fields `assetId`, `url` and validation that `url` matches `^kdesign://asset/[A-Za-z0-9._~-]+/[A-Za-z0-9._~-]+$`.
    - `SyncEnvelopeSchema` with fields `id`, `status`, `remoteDocumentId`, `accountHint`, `localRevision`, `remoteRevision`, `cursor`, `lastSyncedAt`, `diagnostics`.

    Extend `ProjectBundleSchema` with default arrays/optional records:
    - `sourceRecords: z.array(SourceRecordSchema).default([])`
    - `ingestionJobs: z.array(IngestionJobSchema).default([])`
    - `generatedNotes: z.array(GeneratedNoteSchema).default([])`
    - `webSnapshots: z.array(WebSnapshotSchema).default([])`
    - `dataSources: z.array(DataSourceSchema).default([])`
    - `dataBindings: z.array(DataBindingSchema).default([])`
    - `assetLifecycle: z.array(AssetLifecycleEventSchema).default([])`
    - `projectAssetUrls: z.array(ProjectAssetUrlSchema).default([])`
    - `syncEnvelope: SyncEnvelopeSchema.optional()`

    Preserve `schemaVersion: z.literal(1)` and do not remove existing fields.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/schemas.ts contains "export const SourceRecordSchema"
    - packages/editor-core/src/schemas.ts contains "export const GeneratedNoteSchema"
    - packages/editor-core/src/schemas.ts contains "export const WebSnapshotSchema"
    - packages/editor-core/src/schemas.ts contains "export const DataBindingSchema"
    - packages/editor-core/src/schemas.ts contains "export const SyncEnvelopeSchema"
    - packages/editor-core/src/schemas.ts contains "sourceRecords: z.array(SourceRecordSchema).default([])"
    - packages/editor-core/src/schemas.ts contains "syncEnvelope: SyncEnvelopeSchema.optional()"
  </acceptance_criteria>
</task>

<task id="9-01-02" type="auto">
  <name>Create context ingestion helpers</name>
  <files>
    - packages/editor-core/src/context-ingestion.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/context-ingestion.test.ts
  </files>
  <read_first>
    - .planning/phases/09-context-ingestion-live-data-and-assets/09-CONTEXT.md
    - docs/prompts/context-driven-design-agent-prompt.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/generation.ts
    - packages/editor-core/src/normalize.ts
    - packages/editor-core/src/index.ts
  </read_first>
  <action>
    Create `packages/editor-core/src/context-ingestion.ts`. Export:
    - `createSourceRecord(input: { projectId: string; kind: SourceKind; name: string; bytes?: string; sourceUrl?: string; localPath?: string; mimeType?: string; rights?: string; uncertainty?: string; createdAt?: string }): SourceRecord`
    - `createIngestionJob(input: { sourceId: string; status?: SourceParseStatus; diagnostics?: string[]; createdAt?: string }): IngestionJob`
    - `migrateContextAttachmentsToSourceRecords(bundle: ProjectBundle, createdAt?: string): ProjectBundle`
    - `createGeneratedSourceNotes(bundle: ProjectBundle, createdAt?: string): GeneratedNote`
    - `createGeneratedDesignContext(bundle: ProjectBundle, createdAt?: string): GeneratedNote`
    - `createWebSnapshot(input: { bundle: ProjectBundle; sourceId: string; url: string; html?: string; screenshotAssetId?: string; createdAt?: string }): WebSnapshot`
    - `appendSourceRecord(bundle: ProjectBundle, source: SourceRecord): ProjectBundle`
    - `appendGeneratedNote(bundle: ProjectBundle, note: GeneratedNote): ProjectBundle`

    Requirements for helper behavior:
    - Use `stableHash` from `./ids.js` for deterministic ids and hashes.
    - `createGeneratedSourceNotes` must produce a note with `kind: "source-notes"`, `path: "source-notes.md"`, and content containing a markdown heading `# Source Notes`.
    - `createGeneratedDesignContext` must produce a note with `kind: "design-context"`, `path: "design-context.md"`, and content containing `# Design Context`, `## Official sources`, `## Asset paths`, `## Uncertain facts`, and `## Usage rights`.
    - `createWebSnapshot` must set `status: "editable"` only when sanitized/normalized HTML is available and must use existing sanitizer/normalizer paths rather than trusting live DOM. If no HTML is provided, set `status: "referenceOnly"`.
    - `migrateContextAttachmentsToSourceRecords` must preserve legacy `bundle.source.contextAttachments` and add source records without duplicating existing `sourceRecords`.
    - Add `export * from "./context-ingestion.js";` to `packages/editor-core/src/index.ts`.

    Add `packages/editor-core/src/__tests__/context-ingestion.test.ts` with tests for source record creation, legacy attachment migration, generated source notes, generated design context, editable web snapshot from safe HTML, and reference-only snapshot when no HTML is provided.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/context-ingestion.ts exists
    - packages/editor-core/src/context-ingestion.ts contains "export function createSourceRecord("
    - packages/editor-core/src/context-ingestion.ts contains "export function createGeneratedSourceNotes("
    - packages/editor-core/src/context-ingestion.ts contains "export function createGeneratedDesignContext("
    - packages/editor-core/src/context-ingestion.ts contains "export function createWebSnapshot("
    - packages/editor-core/src/index.ts contains "export * from \"./context-ingestion.js\""
    - packages/editor-core/src/__tests__/context-ingestion.test.ts contains "source-notes.md"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/context-ingestion.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="9-01-03" type="auto">
  <name>Create data binding, asset lifecycle, and sync helpers</name>
  <files>
    - packages/editor-core/src/data-bindings.ts
    - packages/editor-core/src/asset-lifecycle.ts
    - packages/editor-core/src/sync.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/data-bindings.test.ts
    - packages/editor-core/src/__tests__/asset-lifecycle.test.ts
    - packages/editor-core/src/__tests__/sync.test.ts
  </files>
  <read_first>
    - .planning/phases/09-context-ingestion-live-data-and-assets/09-CONTEXT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/canvas-graph.ts
    - packages/editor-core/src/patches.ts
    - packages/editor-core/src/index.ts
  </read_first>
  <action>
    Create three editor-core modules and tests.

    In `packages/editor-core/src/data-bindings.ts`, export:
    - `createDataSource(input: { id?: string; kind: DataSourceKind; name: string; sourceId: string; fields: string[]; rows: Array<Record<string, string>>; createdAt?: string }): DataSource`
    - `parseCsvDataSource(input: { name: string; sourceId: string; csv: string; createdAt?: string }): DataSource`
    - `createDataBinding(input: { dataSourceId: string; targetObjectId: string; targetNodeId?: string; fieldMap: Record<string, string>; rowLimit?: number; sourceRevision: string; createdAt?: string }): DataBinding`
    - `previewDataBinding(source: DataSource, binding: DataBinding): { state: DataBindingState; rows: Array<Record<string, string>>; diagnostics: string[] }`
    - `applyDataBindingToBundle(bundle: ProjectBundle, source: DataSource, binding: DataBinding): ProjectBundle`
    Requirements: reject field maps referencing missing source fields, cap preview rows by `rowLimit`, and return `state: "empty"` for empty sources.

    In `packages/editor-core/src/asset-lifecycle.ts`, export:
    - `createProjectAssetUrl(projectId: string, assetId: string): ProjectAssetUrl`
    - `createAssetLifecycleEvent(input: { assetId: string; type: AssetLifecycleEventType; sourceId?: string; previousAssetId?: string; nextAssetId?: string; reason?: string; createdAt?: string }): AssetLifecycleEvent`
    - `replaceAssetReference(bundle: ProjectBundle, input: { previousAssetId: string; nextAsset: AssetRef; reason: string; createdAt?: string }): ProjectBundle`
    - `relinkAssetSource(bundle: ProjectBundle, input: { assetId: string; sourceId: string; reason: string; createdAt?: string }): ProjectBundle`
    Requirements: replacement must add `nextAsset` to `bundle.assets` if missing, add a lifecycle event with `type: "replaced"`, and update `projectAssetUrls` with a stable `kdesign://asset/{projectId}/{assetId}` URL.

    In `packages/editor-core/src/sync.ts`, export:
    - `createSyncEnvelope(input: { bundle: ProjectBundle; remoteDocumentId?: string; accountHint?: string; cursor?: string; createdAt?: string }): SyncEnvelope`
    - `validateSyncEnvelope(bundle: ProjectBundle, envelope: SyncEnvelope): string[]`
    - `markSyncDiverged(bundle: ProjectBundle, reason: string, createdAt?: string): ProjectBundle`
    Requirements: DATA-01 is foundation-only. The sync helpers must not claim hosted account completion; they must validate local revision, remote revision, remote document id, status, and conflict diagnostics.

    Add re-exports for all three modules in `packages/editor-core/src/index.ts`.
    Add tests proving valid CSV/JSON fixture binding, missing field rejection, stable asset URL/replacement history, relink history, valid sync envelope, stale revision diagnostics, and corrupt remote payload rejection.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/data-bindings.ts contains "export function createDataSource("
    - packages/editor-core/src/data-bindings.ts contains "export function previewDataBinding("
    - packages/editor-core/src/asset-lifecycle.ts contains "export function createProjectAssetUrl("
    - packages/editor-core/src/asset-lifecycle.ts contains "export function replaceAssetReference("
    - packages/editor-core/src/sync.ts contains "export function createSyncEnvelope("
    - packages/editor-core/src/sync.ts contains "DATA-01"
    - packages/editor-core/src/index.ts contains "export * from \"./data-bindings.js\""
    - packages/editor-core/src/index.ts contains "export * from \"./asset-lifecycle.js\""
    - packages/editor-core/src/index.ts contains "export * from \"./sync.js\""
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/data-bindings.test.ts src/__tests__/asset-lifecycle.test.ts src/__tests__/sync.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="9-01-04" type="auto">
  <name>Validate persisted context graph integrity</name>
  <files>
    - packages/editor-core/src/integrity.ts
    - packages/editor-core/src/persistence.ts
    - packages/editor-core/src/export.ts
    - packages/editor-core/src/handoff.ts
    - packages/editor-core/src/__tests__/persistence.test.ts
  </files>
  <read_first>
    - .planning/phases/09-context-ingestion-live-data-and-assets/09-CONTEXT.md
    - .planning/phases/08-prototyping-slides-and-ai-variations/08-CONTEXT.md
    - packages/editor-core/src/integrity.ts
    - packages/editor-core/src/persistence.ts
    - packages/editor-core/src/export.ts
    - packages/editor-core/src/handoff.ts
    - packages/editor-core/src/__tests__/persistence.test.ts
  </read_first>
  <action>
    Extend persisted validation so Phase 09 records cannot load in a corrupt state.

    In `packages/editor-core/src/integrity.ts`:
    - Add `assertContextIngestionIntegrity(bundle: ProjectBundle): void` and call it from `assertProjectBundleIntegrity`.
    - Validate every `ingestionJobs[].sourceId`, `generatedNotes[].sourceIds[]`, `webSnapshots[].sourceId`, `webSnapshots[].screenshotAssetId`, `webSnapshots[].canvasObjectIds[]`, `dataSources[].sourceId`, `dataBindings[].dataSourceId`, `dataBindings[].targetObjectId`, optional `dataBindings[].targetNodeId`, `assetLifecycle[].assetId`, optional `assetLifecycle[].sourceId`, optional `previousAssetId`, optional `nextAssetId`, `projectAssetUrls[].assetId`, and `syncEnvelope.localRevision`.
    - Reject `projectAssetUrls[].url` if it does not contain the same project id and asset id as the bundle/record.
    - Reject `dataBindings` whose `sourceRevision` differs from `bundle.baseRevision`.
    - Reject `syncEnvelope.status === "synced"` when `remoteDocumentId` is empty.

    In `packages/editor-core/src/persistence.ts`, ensure legacy bundles are passed through any migration helper before `assertProjectBundleIntegrity`.
    In `packages/editor-core/src/export.ts`, include generated notes in a new exported helper result if needed, but do not change the clean HTML source-of-truth path.
    In `packages/editor-core/src/handoff.ts`, include `source-notes.md`, `design-context.md`, `sourceRecords`, `dataSources`, `assetLifecycle`, and `syncEnvelope` in the handoff package include list or metadata.

    Extend `packages/editor-core/src/__tests__/persistence.test.ts` with persisted adversarial cases:
    - generated note references missing source id -> throws
    - web snapshot references missing screenshot asset -> throws
    - data binding references missing data source -> throws
    - data binding references stale `sourceRevision` -> throws
    - asset lifecycle references missing next asset -> throws
    - project asset URL mismatches asset id -> throws
    - synced sync envelope missing remote document id -> throws
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/integrity.ts contains "function assertContextIngestionIntegrity("
    - packages/editor-core/src/integrity.ts contains "data binding references missing data source"
    - packages/editor-core/src/integrity.ts contains "sync envelope"
    - packages/editor-core/src/persistence.ts contains "assertProjectBundleIntegrity(bundle)"
    - packages/editor-core/src/handoff.ts contains "source-notes.md"
    - packages/editor-core/src/handoff.ts contains "design-context.md"
    - packages/editor-core/src/__tests__/persistence.test.ts contains "generated note references missing source"
    - packages/editor-core/src/__tests__/persistence.test.ts contains "synced sync envelope"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/persistence.test.ts exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] `ProjectBundleSchema` persists Phase 09 source, ingestion, notes, snapshot, data, asset lifecycle, project asset URL, and sync envelope records with defaults.
- [ ] Legacy `source.contextAttachments` migrate into richer source records without losing the original attachment metadata.
- [ ] `source-notes.md` and `design-context.md` are generated from stored source/asset/uncertainty records.
- [ ] Web snapshots are either `editable`, `referenceOnly`, or `blocked`; no live remote DOM is treated as source of truth.
- [ ] Data binding helpers preview repeated content from CSV/static JSON/API fixture records and reject missing fields.
- [ ] Asset replacement/relink helpers preserve audit events and stable `kdesign://asset/{projectId}/{assetId}` URLs.
- [ ] Sync helpers implement only the DATA-01 foundation and do not mark hosted-account sync complete.
- [ ] Persisted corruption probes reject missing source, asset, data, canvas, node, and sync references.
- [ ] All task acceptance criteria verified
- [ ] /sunco:lint passes with zero errors
- [ ] npx tsc --noEmit passes with zero errors
- [ ] pnpm lint passes with zero errors
- [ ] pnpm typecheck passes with zero errors
- [ ] pnpm test passes with zero failures
</done_when>
