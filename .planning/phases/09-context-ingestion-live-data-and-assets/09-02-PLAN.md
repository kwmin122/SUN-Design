---
phase: 9
plan: 02
title: Expose context ingestion, live data, and asset workflows in the web studio
wave: 2
depends_on: [09-01]
files_modified:
  - apps/web/components/editor-shell.tsx
  - apps/web/app/globals.css
  - apps/web/tests/phase-09-context-assets.spec.ts
  - apps/web/tests/phase-09-data-sync.spec.ts
requirements_addressed:
  - CTX-01
  - CTX-02
  - CTX-03
  - DATA-01
  - DATA-02
  - ASSET-01
gap_closure: false
runtime_impact: web UI uses local editor-core helpers and localStorage persistence only; no hosted runtime, worker, auth, billing, browser extension, or object storage is introduced
install_impact: none
docs_update: true
contracts:
  - Web workflow must expose Phase 09 source records, parsed summaries, generated notes, web snapshots, data bindings, asset lifecycle, and sync diagnostics without claiming hosted sync completion
  - URL capture must distinguish editable, referenceOnly, and blocked states with unsafe public URL guards
  - Browser tests must cover happy path and negative path before Phase 09 can be verified
smoke_commands:
  - pnpm lint
  - pnpm typecheck
  - npx tsc --noEmit
  - pnpm test
  - pnpm e2e
---

<objective>
Expose the Phase 09 core foundation as real product workflows in the web studio: context ingestion queue, generated notes viewer, safe web snapshot tool, data-binding panel, asset provenance inspector, and local/mock sync diagnostics.

Requirements fulfilled: CTX-01, CTX-02, CTX-03, DATA-01 foundation-only, DATA-02, ASSET-01
Phase goal contribution: This plan makes the stored context/data/asset foundation visible and testable so Phase 09 is not schema-only. Complexity: L.
</objective>

<tasks>
<task id="9-02-01" type="auto">
  <name>Wire context ingestion queue and notes viewer</name>
  <files>
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </files>
  <read_first>
    - .planning/phases/09-context-ingestion-live-data-and-assets/09-CONTEXT.md
    - .planning/PRODUCT-NORTH-STAR.md
    - docs/prompts/context-driven-design-agent-prompt.md
    - packages/editor-core/src/context-ingestion.ts
    - packages/editor-core/src/schemas.ts
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </read_first>
  <action>
    Update `apps/web/components/editor-shell.tsx` to import the Phase 09 helpers from `@kdesign/editor-core`:
    - `createSourceRecord`
    - `createIngestionJob`
    - `migrateContextAttachmentsToSourceRecords`
    - `createParsedContextArtifact`
    - `parseDocumentSource`
    - `parseSlideDeckSource`
    - `parseSpreadsheetSource`
    - `parseFigmaExportSource`
    - `parseCodebaseFolderManifest`
    - `rejectUnsupportedSource`
    - `createGeneratedSourceNotes`
    - `createGeneratedDesignContext`
    - `appendSourceRecord`
    - `appendParsedContextArtifact`
    - `appendGeneratedNote`

    Add visible UI in the right rail or an existing context/assets section with these test ids:
    - `phase-09-context-queue`
    - `phase-09-add-image-source`
    - `phase-09-add-doc-source`
    - `phase-09-add-pptx-source`
    - `phase-09-add-xlsx-source`
    - `phase-09-add-figma-source`
    - `phase-09-add-codebase-source`
    - `phase-09-add-unsupported-source`
    - `phase-09-parsed-context-summaries`
    - `phase-09-generate-notes`
    - `phase-09-source-notes`
    - `phase-09-design-context`

    Required behavior:
    - Clicking `phase-09-add-image-source` creates a source record with `kind: "image"`, `name: "제품 스크린샷.png"`, `parseStatus: "parsed"`, and `usageStatus: "candidate"`.
    - Clicking `phase-09-add-doc-source` creates a source record with `kind: "document"`, `name: "제품 요구사항.docx"`, and a parsed document summary from deterministic DOCX-like extracted text. The summary must be rendered in `phase-09-parsed-context-summaries`.
    - Clicking `phase-09-add-pptx-source` creates a `slideDeck` source and parsed slide-deck summary from deterministic slide titles/notes. The UI must show at least one slide title.
    - Clicking `phase-09-add-xlsx-source` creates a `spreadsheet` source and parsed table summary from deterministic rows. The UI must show at least one field name or sample row value.
    - Clicking `phase-09-add-figma-source` creates a `figma` source and parsed Figma summary from deterministic frame metadata. The UI must show at least one frame name.
    - Clicking `phase-09-add-codebase-source` creates a `codebase` source and parsed codebase folder manifest summary. The UI must show at least one file path and summary.
    - Clicking `phase-09-add-unsupported-source` calls `rejectUnsupportedSource`, displays a blocked/unsupported diagnostic, and must not create a parsed happy-path artifact.
    - Clicking `phase-09-generate-notes` adds or updates generated notes for `source-notes.md` and `design-context.md`.
    - The queue displays source name, kind, parse status, usage status, and uncertainty/diagnostic text.
    - The parsed summary panel displays artifact title, kind, source id, summary, diagnostics, and the content-bearing fields used by the parser.
    - The notes viewer displays the markdown text for both generated notes.
    - All updates must call `commitBundle` so records survive reload.

    Update `apps/web/app/globals.css` with compact responsive styles for the queue and notes viewer. Use flex layouts and avoid fixed widths that can create body horizontal overflow at 390px, 768px, or 1440px.
  </action>
  <acceptance_criteria>
    - apps/web/components/editor-shell.tsx contains "phase-09-context-queue"
    - apps/web/components/editor-shell.tsx contains "phase-09-add-pptx-source"
    - apps/web/components/editor-shell.tsx contains "phase-09-add-xlsx-source"
    - apps/web/components/editor-shell.tsx contains "phase-09-add-figma-source"
    - apps/web/components/editor-shell.tsx contains "phase-09-add-codebase-source"
    - apps/web/components/editor-shell.tsx contains "phase-09-add-unsupported-source"
    - apps/web/components/editor-shell.tsx contains "phase-09-parsed-context-summaries"
    - apps/web/components/editor-shell.tsx contains "phase-09-generate-notes"
    - apps/web/components/editor-shell.tsx contains "parseFigmaExportSource"
    - apps/web/components/editor-shell.tsx contains "parseCodebaseFolderManifest"
    - apps/web/components/editor-shell.tsx contains "createGeneratedSourceNotes"
    - apps/web/components/editor-shell.tsx contains "createGeneratedDesignContext"
    - apps/web/app/globals.css contains ".phase-09-context"
    - pnpm --filter @kdesign/web typecheck exits 0
  </acceptance_criteria>
</task>

<task id="9-02-02" type="auto">
  <name>Expose web snapshot and asset provenance workflows</name>
  <files>
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </files>
  <read_first>
    - .planning/phases/09-context-ingestion-live-data-and-assets/09-CONTEXT.md
    - .planning/phases/06-canvas-and-component-model/06-CONTEXT.md
    - packages/editor-core/src/context-ingestion.ts
    - packages/editor-core/src/asset-lifecycle.ts
    - packages/editor-core/src/schemas.ts
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </read_first>
  <action>
    Update `apps/web/components/editor-shell.tsx` to import:
    - `createWebSnapshot`
    - `validatePublicSourceUrl`
    - `createProjectAssetUrl`
    - `createAssetLifecycleEvent`
    - `replaceAssetReference`
    - `relinkAssetSource`

    Add UI controls and panels with these test ids:
    - `phase-09-web-snapshot-url`
    - `phase-09-capture-web-snapshot`
    - `phase-09-web-snapshot-list`
    - `phase-09-capture-reference-snapshot`
    - `phase-09-capture-blocked-snapshot`
    - `phase-09-asset-provenance`
    - `phase-09-replace-asset`
    - `phase-09-relink-asset`

    Required behavior:
    - The web snapshot input defaults to `https://example.com/product`.
    - Clicking `phase-09-capture-web-snapshot` first calls `validatePublicSourceUrl`. If valid, it creates a source record with `kind: "webCapture"` and a web snapshot with `status: "editable"` when safe fixture HTML is provided by the UI helper. The fixture HTML must pass through the editor-core snapshot helper, not be saved as live iframe DOM.
    - Clicking `phase-09-capture-reference-snapshot` creates a valid public URL source record without fixture HTML and a web snapshot with `status: "referenceOnly"`.
    - Clicking `phase-09-capture-blocked-snapshot` sets the URL input to `javascript:alert(1)`, creates or displays a blocked ingestion diagnostic, and must not create an editable snapshot.
    - `phase-09-web-snapshot-list` shows URL, status, source id, and whether a sanitized editable section was created.
    - `phase-09-asset-provenance` shows stable `kdesign://asset/...` URLs, asset status, source id, and lifecycle events.
    - Clicking `phase-09-replace-asset` calls `replaceAssetReference` with a deterministic replacement asset named `replacement-product-shot.png`.
    - Clicking `phase-09-relink-asset` calls `relinkAssetSource` for the first source/asset pair when present.
    - All changes persist through `commitBundle`.

    Update styles so the snapshot and asset panels use wrapping rows and scroll-safe text. Long URLs must wrap without increasing `document.body.scrollWidth` beyond `document.body.clientWidth`. The UI must visibly distinguish `editable`, `referenceOnly`, and `blocked` states.
  </action>
  <acceptance_criteria>
    - apps/web/components/editor-shell.tsx contains "phase-09-capture-web-snapshot"
    - apps/web/components/editor-shell.tsx contains "phase-09-capture-reference-snapshot"
    - apps/web/components/editor-shell.tsx contains "phase-09-capture-blocked-snapshot"
    - apps/web/components/editor-shell.tsx contains "validatePublicSourceUrl"
    - apps/web/components/editor-shell.tsx contains "phase-09-asset-provenance"
    - apps/web/components/editor-shell.tsx contains "replaceAssetReference"
    - apps/web/components/editor-shell.tsx contains "kdesign://asset"
    - apps/web/app/globals.css contains "overflow-wrap: anywhere"
    - pnpm --filter @kdesign/web typecheck exits 0
  </acceptance_criteria>
</task>

<task id="9-02-03" type="auto">
  <name>Expose data binding and sync foundation diagnostics</name>
  <files>
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </files>
  <read_first>
    - .planning/phases/09-context-ingestion-live-data-and-assets/09-CONTEXT.md
    - .planning/ROADMAP.md
    - packages/editor-core/src/data-bindings.ts
    - packages/editor-core/src/sync.ts
    - packages/editor-core/src/schemas.ts
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </read_first>
  <action>
    Update `apps/web/components/editor-shell.tsx` to import:
    - `parseCsvDataSource`
    - `createDataBinding`
    - `applyDataBindingToBundle`
    - `previewDataBinding`
    - `createSyncEnvelope`
    - `validateSyncEnvelope`
    - `markSyncDiverged`

    Add UI controls and panels with these test ids:
    - `phase-09-import-csv-data`
    - `phase-09-create-data-binding`
    - `phase-09-data-binding-preview`
    - `phase-09-create-sync-envelope`
    - `phase-09-sync-status`
    - `phase-09-mark-sync-diverged`

    Required behavior:
    - `phase-09-import-csv-data` imports deterministic CSV text: `name,role\n민지,PM\n유진,Designer\n서연,Engineer`.
    - `phase-09-create-data-binding` binds fields to the currently selected canvas object if available, otherwise the first object with a node id. Use `fieldMap: { title: "name", subtitle: "role" }`, `rowLimit: 3`, and `sourceRevision: projectBundle.baseRevision`.
    - `phase-09-data-binding-preview` shows at least the names `민지`, `유진`, and `서연` after binding.
    - `phase-09-create-sync-envelope` creates a sync envelope with `status: "localOnly"`, a deterministic `remoteDocumentId`, account hint `local-mock-account`, and current bundle revision.
    - `phase-09-sync-status` must display "DATA-01 foundation only" so the UI does not claim hosted sync complete.
    - `phase-09-mark-sync-diverged` marks the sync envelope as diverged with a visible diagnostic.
    - All changes persist through `commitBundle`.
  </action>
  <acceptance_criteria>
    - apps/web/components/editor-shell.tsx contains "phase-09-import-csv-data"
    - apps/web/components/editor-shell.tsx contains "phase-09-data-binding-preview"
    - apps/web/components/editor-shell.tsx contains "DATA-01 foundation only"
    - apps/web/components/editor-shell.tsx contains "createSyncEnvelope"
    - apps/web/components/editor-shell.tsx contains "markSyncDiverged"
    - pnpm --filter @kdesign/web typecheck exits 0
  </acceptance_criteria>
</task>

<task id="9-02-04" type="auto">
  <name>Add Phase 09 browser regression coverage</name>
  <files>
    - apps/web/tests/phase-09-context-assets.spec.ts
    - apps/web/tests/phase-09-data-sync.spec.ts
  </files>
  <read_first>
    - .planning/phases/09-context-ingestion-live-data-and-assets/09-CONTEXT.md
    - apps/web/tests/phase-08-agent-output.spec.ts
    - apps/web/tests/phase-07-design-system.spec.ts
    - apps/web/tests/phase-06-canvas.spec.ts
    - apps/web/components/editor-shell.tsx
  </read_first>
  <action>
    Create `apps/web/tests/phase-09-context-assets.spec.ts` with Playwright tests that:
    - open a fresh project by clearing `kdesign.phase01.project.v1`.
    - click `phase-09-add-image-source`, `phase-09-add-doc-source`, `phase-09-add-pptx-source`, `phase-09-add-xlsx-source`, `phase-09-add-figma-source`, and `phase-09-add-codebase-source`.
    - assert `phase-09-parsed-context-summaries` contains a DOCX-like text summary, a slide title, a spreadsheet sample, a Figma frame name, and a codebase file path.
    - click `phase-09-add-unsupported-source` and assert `phase-09-context-queue` contains `unsupported-source-type` or a visible blocked diagnostic.
    - click `phase-09-generate-notes`.
    - assert `phase-09-source-notes` contains `# Source Notes`.
    - assert `phase-09-design-context` contains `# Design Context`.
    - fill or use `phase-09-web-snapshot-url`, click `phase-09-capture-web-snapshot`, and assert `phase-09-web-snapshot-list` contains `editable`.
    - click `phase-09-capture-reference-snapshot` and assert `phase-09-web-snapshot-list` contains `referenceOnly`.
    - click `phase-09-capture-blocked-snapshot` and assert `phase-09-web-snapshot-list` or a visible diagnostic contains `blocked` and `javascript:`.
    - inspect localStorage and assert blocked snapshots do not include `status":"editable"` for the unsafe URL.
    - click `phase-09-replace-asset` and assert `phase-09-asset-provenance` contains `kdesign://asset`.
    - reload and assert notes, snapshot, and asset provenance are still visible.
    - set viewport sizes `{ width: 1440, height: 900 }`, `{ width: 768, height: 900 }`, and `{ width: 390, height: 844 }`; for each, assert `document.body.scrollWidth <= document.body.clientWidth`.

    Create `apps/web/tests/phase-09-data-sync.spec.ts` with Playwright tests that:
    - open a fresh project by clearing `kdesign.phase01.project.v1`.
    - click `phase-09-import-csv-data` and `phase-09-create-data-binding`.
    - assert `phase-09-data-binding-preview` contains `민지`, `유진`, and `서연`.
    - click `phase-09-create-sync-envelope` and assert `phase-09-sync-status` contains `DATA-01 foundation only`.
    - click `phase-09-mark-sync-diverged` and assert `phase-09-sync-status` contains `diverged`.
    - inspect localStorage and assert it contains `dataSources`, `dataBindings`, and `syncEnvelope`, and does not contain `iframe.contentDocument`.
    - reload and assert the data binding preview and sync status survive reload.
  </action>
  <acceptance_criteria>
    - apps/web/tests/phase-09-context-assets.spec.ts exists
    - apps/web/tests/phase-09-context-assets.spec.ts contains "phase-09-source-notes"
    - apps/web/tests/phase-09-context-assets.spec.ts contains "phase-09-capture-blocked-snapshot"
    - apps/web/tests/phase-09-context-assets.spec.ts contains "referenceOnly"
    - apps/web/tests/phase-09-context-assets.spec.ts contains "javascript:"
    - apps/web/tests/phase-09-context-assets.spec.ts contains "document.body.scrollWidth <= document.body.clientWidth"
    - apps/web/tests/phase-09-data-sync.spec.ts exists
    - apps/web/tests/phase-09-data-sync.spec.ts contains "DATA-01 foundation only"
    - apps/web/tests/phase-09-data-sync.spec.ts contains "iframe.contentDocument"
    - pnpm e2e -- apps/web/tests/phase-09-context-assets.spec.ts apps/web/tests/phase-09-data-sync.spec.ts exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] The web studio exposes a visible Phase 09 context ingestion queue with source status and diagnostics.
- [ ] The web studio exposes parsed context summaries for DOCX-like document text, PPTX-like slide data, XLSX/CSV-like rows, Figma frame metadata, and codebase folder manifests.
- [ ] Unsupported source types are visibly blocked and covered by browser tests.
- [ ] `source-notes.md` and `design-context.md` are generated, visible, persisted, and reloadable.
- [ ] Safe web snapshot capture creates an editable/reference status without using live iframe DOM as source of truth.
- [ ] Unsafe URL capture is visibly blocked and tested; public URL without fixture HTML becomes `referenceOnly`.
- [ ] Asset provenance shows stable `kdesign://asset/...` URLs and replacement/relink lifecycle events.
- [ ] CSV/static fixture data can bind to a canvas object and preview repeated Korean content.
- [ ] Sync UI clearly says `DATA-01 foundation only` and shows local/mock divergence diagnostics without claiming hosted sync complete.
- [ ] Desktop, tablet, and mobile browser checks show no body horizontal overflow.
- [ ] All task acceptance criteria verified
- [ ] /sunco:lint passes with zero errors
- [ ] npx tsc --noEmit passes with zero errors
- [ ] pnpm lint passes with zero errors
- [ ] pnpm typecheck passes with zero errors
- [ ] pnpm test passes with zero failures
- [ ] pnpm e2e passes with zero failures
</done_when>
