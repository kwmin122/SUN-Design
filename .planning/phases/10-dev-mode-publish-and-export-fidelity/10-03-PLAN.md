---
phase: 10
plan: 03
title: Expose Dev Mode, publish, export, and roundtrip workflows in the web studio
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - apps/web/components/dev-mode-panel.tsx
  - apps/web/components/export-publish-panel.tsx
  - apps/web/components/editor-shell.tsx
  - apps/web/app/globals.css
  - apps/web/tests/phase-10-dev-mode.spec.ts
  - apps/web/tests/phase-10-export-publish.spec.ts
  - CLAUDE.md
requirements_addressed:
  - DEV-01
  - DEV-02
  - DEV-03
  - DEV-04
  - EXP-09
  - EXP-10
  - EXP-11
  - EXP-12
  - SLIDE-04
  - SLIDE-05
gap_closure: false
runtime_impact: web UI uses local editor-core/export-worker records and local persistence only; no hosted production service, auth, CDN, billing, or multiplayer runtime is introduced
install_impact: none beyond dependencies added by 10-02
docs_update: true
contracts:
  - Top Export and right-rail export controls must trigger real stored-state export records, not inert affordances
  - Dev Mode UI must read stored inspect reports and rendered rect snapshots, not live iframe DOM as source of truth
  - UI copy must state static publish/local preview boundary and must not claim hosted account/CDN publish
  - Browser tests must cover reload, desktop/tablet/mobile overflow, and negative roundtrip conflict paths
smoke_commands:
  - pnpm lint
  - pnpm typecheck
  - npx tsc --noEmit
  - pnpm test
  - pnpm e2e
---

<objective>
Expose Phase 10 core and export-worker capabilities in the product shell: Dev Mode inspect/code snippets, ready-for-dev, version diff, asset downloads, export queue, static publish preview, PPTX/animation export, and code-agent roundtrip.

Requirements fulfilled: DEV-01, DEV-02, DEV-03, DEV-04, EXP-09, EXP-10, EXP-11, EXP-12, SLIDE-04, SLIDE-05
Phase goal contribution: This plan makes Phase 10 usable and verifiable in the browser so the work is not schema-only or worker-only. Complexity: L.
</objective>

<tasks>
<task id="10-03-01" type="auto">
  <name>Create Dev Mode product panel</name>
  <files>
    - apps/web/components/dev-mode-panel.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
    - apps/web/tests/phase-10-dev-mode.spec.ts
  </files>
  <read_first>
    - .planning/phases/10-dev-mode-publish-and-export-fidelity/10-CONTEXT.md
    - .planning/PRODUCT-NORTH-STAR.md
    - packages/editor-core/src/dev-mode.ts
    - packages/editor-core/src/schemas.ts
    - apps/web/components/editor-shell.tsx
    - apps/web/components/canvas-object-inspector.tsx
    - apps/web/components/component-playground-panel.tsx
    - apps/web/app/globals.css
  </read_first>
  <action>
    Create `apps/web/components/dev-mode-panel.tsx`. Export `DevModePanel` with props:
    - `bundle: ProjectBundle`
    - `selectedObject: CanvasObject | undefined`
    - `onCreateInspectReport(objectId: string): void`
    - `onCreateCodeSnippet(objectId: string, kind: DevCodeSnippetKind): void`
    - `onMarkReady(objectId: string): void`
    - `onCreateVersionDiff(objectId: string): void`
    - `onCreateAssetDownload(assetId: string): void`

    The panel must render these test ids:
    - `phase-10-dev-mode-panel`
    - `phase-10-create-inspect-report`
    - `phase-10-inspect-measurements`
    - `phase-10-token-references`
    - `phase-10-accessibility-notes`
    - `phase-10-component-metadata`
    - `phase-10-prototype-metadata`
    - `phase-10-code-snippet-kind`
    - `phase-10-create-code-snippet`
    - `phase-10-code-snippets`
    - `phase-10-mark-ready`
    - `phase-10-ready-state`
    - `phase-10-create-version-diff`
    - `phase-10-version-diff`
    - `phase-10-create-asset-download`
    - `phase-10-asset-downloads`

    Update `apps/web/components/editor-shell.tsx`:
    - Import `DevModePanel`.
    - Import `createDevModeInspectReport`, `appendDevModeReport`, `createDevCodeSnippet`, `appendDevCodeSnippet`, `markReadyForDev`, `markReadyForDevChanged`, `createVersionDiffRecord`, `appendVersionDiffRecord`, `createAssetDownloadRecord`, and `appendAssetDownloadRecord` from `@kdesign/editor-core`.
    - Add a Dev Mode toggle in the existing top tools with test id `phase-10-dev-mode-toggle`.
    - When Dev Mode is active and a selected canvas object exists, render `DevModePanel` in the right rail.
    - For `createDevModeInspectReport`, pass rendered rect data from the existing selected preview rect when available; otherwise let the core helper produce the `missing_rendered_rect` accessibility note.
    - `phase-10-mark-ready` must persist a ready marker at the current base revision.
    - If the bundle changes after a ready marker, call `markReadyForDevChanged` before saving.
    - Asset download actions must use the first selected-object asset id, or the first `bundle.assets` id when the selected object has no asset.

    Update `apps/web/app/globals.css` with `.phase-10-dev-mode`, `.dev-mode-grid`, `.dev-code-block`, `.ready-state-badge`, and wrapping rules for long code/token values. No element may force horizontal body overflow at 390px, 768px, or 1440px.

    Add `apps/web/tests/phase-10-dev-mode.spec.ts`:
    - Load the app and wait for `diagnostics-readiness` to contain `ready`.
    - Select or use the first canvas object.
    - Click `phase-10-dev-mode-toggle`.
    - Create an inspect report and assert measurements, token references, and accessibility notes render.
    - Create `css`, `tailwind`, `reactProps`, and `tokenReference` snippets and assert snippets render.
    - Mark ready, create a version diff, create an asset download record, reload, and assert the records persist.
    - Check `document.body.scrollWidth <= document.body.clientWidth` at desktop, tablet, and mobile viewport widths.
  </action>
  <acceptance_criteria>
    - apps/web/components/dev-mode-panel.tsx exists
    - apps/web/components/dev-mode-panel.tsx contains "phase-10-dev-mode-panel"
    - apps/web/components/dev-mode-panel.tsx contains "phase-10-create-code-snippet"
    - apps/web/components/dev-mode-panel.tsx contains "phase-10-mark-ready"
    - apps/web/components/dev-mode-panel.tsx contains "phase-10-version-diff"
    - apps/web/components/editor-shell.tsx contains "phase-10-dev-mode-toggle"
    - apps/web/components/editor-shell.tsx contains "createDevModeInspectReport"
    - apps/web/components/editor-shell.tsx contains "markReadyForDev"
    - apps/web/app/globals.css contains ".phase-10-dev-mode"
    - apps/web/tests/phase-10-dev-mode.spec.ts contains "phase-10-dev-mode-toggle"
    - pnpm e2e -- apps/web/tests/phase-10-dev-mode.spec.ts exits 0
  </acceptance_criteria>
</task>

<task id="10-03-02" type="auto">
  <name>Create export, publish, and roundtrip product panel</name>
  <files>
    - apps/web/components/export-publish-panel.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
    - apps/web/tests/phase-10-export-publish.spec.ts
  </files>
  <read_first>
    - .planning/phases/10-dev-mode-publish-and-export-fidelity/10-CONTEXT.md
    - .planning/PRODUCT-NORTH-STAR.md
    - packages/editor-core/src/export-fidelity.ts
    - packages/editor-core/src/code-roundtrip.ts
    - apps/export-worker/src/export-worker.ts
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </read_first>
  <action>
    Create `apps/web/components/export-publish-panel.tsx`. Export `ExportPublishPanel` with props:
    - `bundle: ProjectBundle`
    - `onCreateExport(kind: ExportKind): void`
    - `onCreatePublishPreview(): void`
    - `onCreateCodeRoundtrip(runtime: AgentRuntime): void`
    - `onImportRoundtripConflict(): void`

    The panel must render these test ids:
    - `phase-10-export-publish-panel`
    - `phase-10-export-html`
    - `phase-10-export-zip`
    - `phase-10-export-png`
    - `phase-10-export-pdf`
    - `phase-10-export-pptx-raster`
    - `phase-10-export-pptx-editable`
    - `phase-10-export-gif`
    - `phase-10-export-mp4`
    - `phase-10-export-artifacts`
    - `phase-10-export-verifications`
    - `phase-10-create-publish-preview`
    - `phase-10-publish-preview`
    - `phase-10-code-roundtrip-runtime`
    - `phase-10-create-code-roundtrip`
    - `phase-10-import-roundtrip-conflict`
    - `phase-10-code-roundtrip-results`

    Update `apps/web/components/editor-shell.tsx`:
    - Import `ExportPublishPanel`.
    - Import editor-core `createExportArtifactRecord`, `createExportVerification`, `appendExportArtifact`, `createPublishPreview`, `appendPublishPreview`, `createCodeRoundtripPackage`, `appendCodeRoundtripPackage`, `validateCodeRoundtripImport`, and `appendCodeRoundtripImport`.
    - Wire top Export button so it calls the same stored-state export path as `phase-10-export-html`, not the old metadata-only path.
    - Because the browser shell cannot run the Node export worker directly, create local deterministic artifact records that mirror worker outputs and clearly label diagnostics with `web-local-record`. Do not claim files are downloaded from the browser unless a real artifact record exists.
    - `phase-10-export-html`, `phase-10-export-zip`, `phase-10-export-png`, `phase-10-export-pdf`, `phase-10-export-gif`, and `phase-10-export-mp4` must create `exportJobs`, `exportArtifacts`, and at least one `exportVerification` record each.
    - `phase-10-export-pptx-raster` must create a PPTX export artifact with diagnostic `pptx-mode:rasterized`.
    - `phase-10-export-pptx-editable` must create a PPTX export artifact with diagnostic `pptx-mode:editableSubset`.
    - `phase-10-create-publish-preview` must create a `kdesign://publish/...` preview record referencing existing artifacts.
    - `phase-10-create-code-roundtrip` must create a code roundtrip package for the selected runtime.
    - `phase-10-import-roundtrip-conflict` must create a rejected or conflict import result with diagnostics containing `source-revision-mismatch`.
    - Records must persist through reload.

    Update `apps/web/app/globals.css` with `.phase-10-export`, `.export-artifact-row`, `.verification-pill`, `.publish-preview-row`, and `.roundtrip-result-row`. Long filenames, hashes, and package manifests must wrap.

    Add `apps/web/tests/phase-10-export-publish.spec.ts`:
    - Load the app and wait for readiness.
    - Open the Phase 10 export panel.
    - Trigger HTML, ZIP, PNG, PDF, raster PPTX, editable PPTX, GIF, and MP4 export actions.
    - Assert artifact rows include `.html`, `.zip`, `.png`, `.pdf`, `.pptx`, `.gif`, and `.mp4`.
    - Assert verification rows include `signature` and `manifest`.
    - Create publish preview and assert URL begins with `kdesign://publish/`.
    - Create code roundtrip package, then trigger conflict import and assert `source-revision-mismatch` appears.
    - Reload and assert artifacts, publish previews, and roundtrip results persist.
    - Check no body horizontal overflow at 390px, 768px, and 1440px.
  </action>
  <acceptance_criteria>
    - apps/web/components/export-publish-panel.tsx exists
    - apps/web/components/export-publish-panel.tsx contains "phase-10-export-publish-panel"
    - apps/web/components/export-publish-panel.tsx contains "phase-10-export-pptx-editable"
    - apps/web/components/export-publish-panel.tsx contains "phase-10-export-mp4"
    - apps/web/components/export-publish-panel.tsx contains "phase-10-create-publish-preview"
    - apps/web/components/export-publish-panel.tsx contains "phase-10-import-roundtrip-conflict"
    - apps/web/components/editor-shell.tsx contains "createPublishPreview"
    - apps/web/components/editor-shell.tsx contains "validateCodeRoundtripImport"
    - apps/web/app/globals.css contains ".phase-10-export"
    - apps/web/tests/phase-10-export-publish.spec.ts contains "kdesign://publish/"
    - apps/web/tests/phase-10-export-publish.spec.ts contains "source-revision-mismatch"
    - pnpm e2e -- apps/web/tests/phase-10-export-publish.spec.ts exits 0
  </acceptance_criteria>
</task>

<task id="10-03-03" type="auto">
  <name>Update project guidance and full workflow regressions</name>
  <files>
    - CLAUDE.md
    - apps/web/tests/phase-10-dev-mode.spec.ts
    - apps/web/tests/phase-10-export-publish.spec.ts
  </files>
  <read_first>
    - .planning/phases/10-dev-mode-publish-and-export-fidelity/10-CONTEXT.md
    - .planning/STATE.md
    - CLAUDE.md
    - apps/web/tests/phase-10-dev-mode.spec.ts
    - apps/web/tests/phase-10-export-publish.spec.ts
  </read_first>
  <action>
    Update `CLAUDE.md` current phase section:
    - Current phase must be `Phase 10 — Dev Mode, Publish, and Export Fidelity`.
    - Phase status must be `planned; ready for execution`.
    - Next SUNCO action must be `/sunco:execute 10`.
    - The scope paragraph must state that Phase 10 implements Dev Mode inspect/code/readiness/version diff, real stored-state export materialization, static publish preview, code-agent roundtrip, and PPTX/animation export foundations while deferring full hosted production publish, full Figma export, and unrestricted PPT authoring.

    Extend `apps/web/tests/phase-10-dev-mode.spec.ts` and `apps/web/tests/phase-10-export-publish.spec.ts` with a combined persistence assertion:
    - After running both workflows, read `localStorage.getItem("kdesign-project-bundle")`.
    - Assert JSON contains `devModeReports`, `readyForDevMarkers`, `exportArtifacts`, `publishPreviews`, and `codeRoundtripPackages`.
    - Assert JSON does not contain `data-preview-bridge`, `selection-overlay`, or `live iframe DOM`.

    Confirm full gate commands pass after implementation:
    - `pnpm lint`
    - `pnpm typecheck`
    - `npx tsc --noEmit`
    - `pnpm test`
    - `pnpm e2e`
  </action>
  <acceptance_criteria>
    - CLAUDE.md contains "Phase 10 — Dev Mode, Publish, and Export Fidelity"
    - CLAUDE.md contains "Next SUNCO action: `/sunco:execute 10`"
    - apps/web/tests/phase-10-dev-mode.spec.ts contains "devModeReports"
    - apps/web/tests/phase-10-export-publish.spec.ts contains "exportArtifacts"
    - apps/web/tests/phase-10-export-publish.spec.ts contains "codeRoundtripPackages"
    - apps/web/tests/phase-10-export-publish.spec.ts contains "data-preview-bridge"
    - pnpm lint exits 0
    - pnpm typecheck exits 0
    - npx tsc --noEmit exits 0
    - pnpm test exits 0
    - pnpm e2e exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] Dev Mode panel exposes inspect measurements, token references, accessibility notes, component metadata, prototype metadata, code snippets, ready state, version diff, and asset downloads.
- [ ] Export/publish panel exposes HTML, ZIP, PNG, PDF, raster PPTX, editable PPTX subset, GIF, MP4, static publish preview, and code roundtrip flows.
- [ ] Top Export button is wired to the Phase 10 stored-state export path and is not inert or metadata-only.
- [ ] Browser tests verify reload persistence and no horizontal overflow at desktop, tablet, and mobile widths.
- [ ] CLAUDE.md reflects Phase 10 planned status and next `/sunco:execute 10` route.
- [ ] All task acceptance criteria verified.
- [ ] /sunco:lint passes with zero errors.
- [ ] npx tsc --noEmit passes with zero errors.
- [ ] pnpm lint passes with zero errors.
- [ ] pnpm typecheck passes with zero errors.
- [ ] pnpm test passes with zero failures.
- [ ] pnpm e2e passes with zero failures.
</done_when>
