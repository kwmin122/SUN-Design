---
phase: 6
plan: 01
title: Core Canvas State and Operations
wave: 1
depends_on: []
files_modified:
  - packages/editor-core/src/schemas.ts
  - packages/editor-core/src/canvas-graph.ts
  - packages/editor-core/src/canvas-operations.ts
  - packages/editor-core/src/canvas-components.ts
  - packages/editor-core/src/persistence.ts
  - packages/editor-core/src/export.ts
  - packages/editor-core/src/handoff.ts
  - packages/editor-core/src/index.ts
  - packages/editor-core/src/__tests__/canvas-graph.test.ts
  - packages/editor-core/src/__tests__/canvas-operations.test.ts
  - packages/editor-core/src/__tests__/canvas-components.test.ts
  - packages/editor-core/src/__tests__/schemas.test.ts
  - packages/editor-core/src/__tests__/export.test.ts
  - packages/editor-core/src/__tests__/handoff.test.ts
requirements_addressed:
  - CANVAS-01
  - CANVAS-03
  - CANVAS-04
  - CANVAS-05
gap_closure: false
---

<objective>
Build the versioned canvas object graph, typed canvas operation log, layout constraint validation, local component model, portable handoff inclusion, and v1 bundle compatibility path in `packages/editor-core`.

Complexity: L
Requirements fulfilled: CANVAS-01, CANVAS-03, CANVAS-04, CANVAS-05
Phase goal contribution: This creates the durable state foundation that lets normalized HTML nodes become explicit canvas objects without replacing the HTML-first source-of-truth family.
</objective>

<tasks>
<task id="6-01-01" type="auto">
  <name>Add canvas and component schemas</name>
  <files>
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/__tests__/schemas.test.ts
  </files>
  <read_first>
    - .planning/phases/06-canvas-and-component-model/06-CONTEXT.md
    - .planning/phases/06-canvas-and-component-model/06-RESEARCH.md
    - CLAUDE.md
    - .planning/PRODUCT-NORTH-STAR.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/research/COMPETITIVE-GAP-REVIEW.md
    - .planning/research/ARCHITECTURE.md
    - .planning/research/FEATURES.md
    - .planning/research/PITFALLS.md
    - .planning/research/SUMMARY.md
    - docs/guides/coding-principles.md
    - docs/prompts/context-driven-design-agent-prompt.md
    - docs/superpowers/specs/2026-04-27-k-design-studio-design.md
    - .planning/phases/01-safe-html-document-foundation/01-CONTEXT.md
    - .planning/phases/02-direct-editing-comments-and-tweaks/02-CONTEXT.md
    - .planning/phases/03-prompt-creation-modes-assets-and-korean-presets/03-CONTEXT.md
    - .planning/phases/04-responsive-preview-and-export-fidelity/04-CONTEXT.md
    - .planning/phases/05-design-systems-sharing-and-agent-agnostic-handoff/05-CONTEXT.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/__tests__/schemas.test.ts
  </read_first>
  <action>
    Extend `packages/editor-core/src/schemas.ts` with these exported Zod schemas and types:
    - `CanvasObjectKindSchema = z.enum(["page", "artboard", "frame", "section", "componentInstance", "slot", "text", "image", "button", "vectorLike", "unknown"])`
    - `CanvasLayoutModeSchema = z.enum(["none", "block", "flex", "grid"])`
    - `CanvasResizeModeSchema = z.enum(["fixed", "hug", "fill"])`
    - `CanvasPinnedEdgesSchema = z.object({ top: z.boolean().default(false), right: z.boolean().default(false), bottom: z.boolean().default(false), left: z.boolean().default(false) })`
    - `CanvasLayoutSchema` with optional `display`, `flexDirection`, `gap`, `padding`, `alignItems`, `justifyContent`, `gridTemplateColumns`, and `breakpoint` string fields. Allowed `display` values must be `"block"`, `"flex"`, and `"grid"`.
    - `CanvasConstraintsSchema` with optional numeric `x`, `y`, `width`, `height`, `minWidth`, `minHeight`, `maxWidth`, `maxHeight`; `resizeMode`; `pinned`; and `layout`.
    - `CanvasObjectSchema` with `id`, `kind`, `name`, optional `nodeId`, optional `parentId`, `childIds: string[]`, `locked: boolean`, `hidden: boolean`, optional `constraints`, optional `componentInstanceId`.
    - `CanvasComponentPropSchema` with `id`, `name`, `kind: z.enum(["text", "color", "number", "boolean", "slot"])`, optional `defaultValue`.
    - `CanvasComponentVariantSchema` with `id`, `name`, `props: z.record(z.string(), z.unknown()).default({})`.
    - `CanvasComponentDefinitionSchema` with `id`, `name`, `sourceObjectId`, `slotObjectIds: string[]`, `props: CanvasComponentPropSchema[]`, `variants: CanvasComponentVariantSchema[]`, and `createdAt`.
    - `CanvasComponentInstanceSchema` with `id`, `componentId`, `objectId`, optional `variantId`, `state: z.enum(["default", "hover", "pressed", "disabled"]).default("default")`, `overrides: z.record(z.string(), z.unknown()).default({})`, and `detached: z.boolean().default(false)`.
    - `CanvasGuideSchema` with `id`, `axis: z.enum(["x", "y"])`, `position`, and optional `label`.
    - `CanvasGraphSchema` with `version: z.literal(1)`, `rootObjectIds`, `objects`, `components`, `instances`, `guides`, and `updatedAt`.
    - `CanvasOperationSchema` with `id`, `op`, `objectId`, `value`, `source: z.enum(["canvas", "tweaks", "agent", "system"])`, `baseRevision`, and `createdAt`. Supported `op` values must include `"setObjectName"`, `"setObjectVisibility"`, `"setObjectLock"`, `"reorderObject"`, `"groupObjects"`, `"ungroupObjects"`, `"setLayoutConstraints"`, `"createComponent"`, `"createComponentInstance"`, `"updateComponentOverride"`, and `"detachComponentInstance"`.
    Add exported TypeScript types for each schema.
    Add `canvasGraph: CanvasGraphSchema.optional()` and `canvasOperations: z.array(CanvasOperationSchema).default([])` to `ProjectBundleSchema`.
    Update `packages/editor-core/src/__tests__/schemas.test.ts` so the existing minimal v1-like bundle still parses successfully without `canvasGraph`, and add a new test named `accepts a ProjectBundle with canvas graph and canvas operations`.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/schemas.ts contains "export const CanvasGraphSchema"
    - packages/editor-core/src/schemas.ts contains "export const CanvasOperationSchema"
    - packages/editor-core/src/schemas.ts contains "canvasGraph: CanvasGraphSchema.optional()"
    - packages/editor-core/src/schemas.ts contains "canvasOperations: z.array(CanvasOperationSchema).default([])"
    - packages/editor-core/src/__tests__/schemas.test.ts contains "accepts a ProjectBundle with canvas graph and canvas operations"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/schemas.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="6-01-02" type="auto">
  <name>Derive canvas graph from edit graph</name>
  <files>
    - packages/editor-core/src/canvas-graph.ts
    - packages/editor-core/src/persistence.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/canvas-graph.test.ts
  </files>
  <read_first>
    - .planning/phases/06-canvas-and-component-model/06-CONTEXT.md
    - .planning/phases/06-canvas-and-component-model/06-RESEARCH.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/normalize.ts
    - packages/editor-core/src/persistence.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/normalize.test.ts
    - packages/editor-core/src/__tests__/persistence.test.ts
  </read_first>
  <action>
    Create `packages/editor-core/src/canvas-graph.ts` with these exports:
    - `deriveCanvasGraph(bundle: ProjectBundle, updatedAt = bundle.updatedAt): CanvasGraph`
    - `ensureCanvasGraph(bundle: ProjectBundle): ProjectBundle`
    - `listCanvasObjects(graph: CanvasGraph): CanvasObject[]`
    - `findCanvasObjectByNodeId(graph: CanvasGraph, nodeId: string): CanvasObject | undefined`
    - `getCanvasObject(graph: CanvasGraph, objectId: string): CanvasObject | undefined`
    `deriveCanvasGraph` must create a root page object with id `obj_page_${bundle.id}`, an artboard object with id `obj_artboard_${bundle.id}`, and one object per edit node with id `obj_${node.id}`. Map edit node kinds this way: `frame -> frame`, `block -> section`, `text -> text`, `image -> image`, `button -> button`, `control -> unknown`, `decorative -> vectorLike`, `unknown -> unknown`. Root edit nodes become children of the artboard. Nested edit nodes use their edit node parent as the canvas parent when available. Each object must have `childIds`, `locked: false`, `hidden: false`, and a human-readable `name` built from `textPreview` or `${kind} ${tagName}`.
    `ensureCanvasGraph` must return the input bundle unchanged when `bundle.canvasGraph` exists, otherwise return `ProjectBundleSchema.parse({ ...bundle, canvasGraph: deriveCanvasGraph(bundle) })`.
    Update `packages/editor-core/src/persistence.ts` so `parseProjectBundleJson` parses JSON, validates it with `ProjectBundleSchema`, then returns `ensureCanvasGraph(parsed)`. Keep `serializeProjectBundle` validating through `ProjectBundleSchema.parse`.
    Add `export * from "./canvas-graph.js";` to `packages/editor-core/src/index.ts`.
    Create tests proving v1 JSON without `canvasGraph` loads with a graph, root page/artboard ids are deterministic, node objects reference existing edit node ids, and `findCanvasObjectByNodeId` returns the expected object.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/canvas-graph.ts exists
    - packages/editor-core/src/canvas-graph.ts contains "export function deriveCanvasGraph("
    - packages/editor-core/src/canvas-graph.ts contains "obj_page_"
    - packages/editor-core/src/persistence.ts contains "ensureCanvasGraph"
    - packages/editor-core/src/index.ts contains "export * from \"./canvas-graph.js\""
    - packages/editor-core/src/__tests__/canvas-graph.test.ts contains "derives a deterministic page and artboard graph"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/canvas-graph.test.ts src/__tests__/persistence.test.ts exits 0
  </acceptance_criteria>
</task>

<task id="6-01-03" type="auto">
  <name>Apply typed canvas operations and materialize layout</name>
  <files>
    - packages/editor-core/src/canvas-operations.ts
    - packages/editor-core/src/export.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/canvas-operations.test.ts
    - packages/editor-core/src/__tests__/export.test.ts
  </files>
  <read_first>
    - .planning/phases/06-canvas-and-component-model/06-CONTEXT.md
    - .planning/phases/06-canvas-and-component-model/06-RESEARCH.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/canvas-graph.ts
    - packages/editor-core/src/patches.ts
    - packages/editor-core/src/export.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/patches.test.ts
    - packages/editor-core/src/__tests__/export.test.ts
  </read_first>
  <action>
    Create `packages/editor-core/src/canvas-operations.ts` with these exports:
    - `applyCanvasOperationToBundle(bundle: ProjectBundle, operation: CanvasOperation): ProjectBundle`
    - `applyCanvasOperationsToBundle(bundle: ProjectBundle, operations: CanvasOperation[]): ProjectBundle`
    - `isLayoutValueSafe(value: string): boolean`
    - `layoutConstraintsToStyle(constraints: CanvasConstraints): Record<string, string>`
    `applyCanvasOperationToBundle` must call `ensureCanvasGraph(bundle)` first. It must append each accepted operation to `canvasOperations`, update `updatedAt`, and parse the result with `ProjectBundleSchema`.
    Implement operation behavior:
    - `setObjectName`: `value` is `{ name: string }`; update object name.
    - `setObjectVisibility`: `value` is `{ hidden: boolean }`; update object hidden and, when `nodeId` exists, apply `setVisibility` edit patch with the inverse visibility.
    - `setObjectLock`: `value` is `{ locked: boolean }`; update lock state.
    - `reorderObject`: `value` is `{ parentId: string, index: number }`; move object id within parent `childIds`.
    - `setLayoutConstraints`: `value` is `{ constraints: CanvasConstraints }`; validate display/gap/padding/grid values and update `object.constraints`. When `nodeId` exists, materialize safe layout fields to an edit `setStyle` patch using `display`, `gap`, `padding`, `align-items`, `justify-content`, `grid-template-columns`, `width`, and `height`.
    - `groupObjects`: `value` is `{ name: string, childObjectIds: string[] }`; create a frame object with id `obj_group_${operation.id}` and move listed children under it.
    - `ungroupObjects`: move children to the group parent and remove the group object.
    Reject unknown objects, locked object mutations except `setObjectLock`, unsafe layout strings containing `<`, `>`, `;`, `javascript:`, `expression(`, or `url(`, unknown parent ids, invalid reorder indexes, and group operations with missing children.
    Update `packages/editor-core/src/export.ts` so `createStandaloneHtml` calls `ensureCanvasGraph` before stripping editor attributes. Do not add editor-only canvas metadata to clean HTML.
    Add `export * from "./canvas-operations.js";` to `packages/editor-core/src/index.ts`.
    Add unit tests for rename, hide materialization, layout materialization, group/ungroup, locked-object rejection, unknown-object rejection, and unsafe layout value rejection.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/canvas-operations.ts exists
    - packages/editor-core/src/canvas-operations.ts contains "export function applyCanvasOperationToBundle("
    - packages/editor-core/src/canvas-operations.ts contains "setLayoutConstraints"
    - packages/editor-core/src/canvas-operations.ts contains "unsafe layout"
    - packages/editor-core/src/index.ts contains "export * from \"./canvas-operations.js\""
    - packages/editor-core/src/__tests__/canvas-operations.test.ts contains "rejects unsafe layout values"
    - packages/editor-core/src/__tests__/canvas-operations.test.ts contains "materializes layout constraints to normalized HTML"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/canvas-operations.test.ts src/__tests__/export.test.ts exits 0
  </acceptance_criteria>
</task>
<task id="6-01-04" type="auto">
  <name>Add component helpers and portable handoff records</name>
  <files>
    - packages/editor-core/src/canvas-components.ts
    - packages/editor-core/src/canvas-operations.ts
    - packages/editor-core/src/handoff.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/canvas-components.test.ts
    - packages/editor-core/src/__tests__/handoff.test.ts
  </files>
  <read_first>
    - .planning/phases/06-canvas-and-component-model/06-CONTEXT.md
    - .planning/phases/06-canvas-and-component-model/06-RESEARCH.md
    - .planning/phases/05-design-systems-sharing-and-agent-agnostic-handoff/05-CONTEXT.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/canvas-graph.ts
    - packages/editor-core/src/canvas-operations.ts
    - packages/editor-core/src/handoff.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/handoff.test.ts
  </read_first>
  <action>
    Create `packages/editor-core/src/canvas-components.ts` with these exports:
    - `createLocalComponentDefinition(input: { graph: CanvasGraph; sourceObjectId: string; name: string; createdAt: string }): CanvasComponentDefinition`
    - `createLocalComponentInstance(input: { graph: CanvasGraph; componentId: string; targetObjectId: string; variantId?: string; createdAt: string }): CanvasComponentInstance`
    - `updateComponentOverride(instance: CanvasComponentInstance, key: string, value: unknown): CanvasComponentInstance`
    - `resolveComponentVariant(component: CanvasComponentDefinition, variantId: string | undefined): CanvasComponentVariant | undefined`
    - `summarizeComponentInstance(graph: CanvasGraph, instanceId: string): { componentName: string; objectName: string; variantName: string; state: string; overrideCount: number }`
    `createLocalComponentDefinition` must reject unknown source objects, create two default props named `label` and `tone`, create two variants named `Default` and `Emphasis`, set `sourceObjectId`, and set `slotObjectIds` from the source object's `childIds`.
    `createLocalComponentInstance` must reject unknown components, unknown target objects, and missing variant ids. It must return state `default`, `detached: false`, and empty overrides.
    `updateComponentOverride` must return a new instance with merged overrides and must reject an empty override key.
    Update `packages/editor-core/src/canvas-operations.ts` to call these helpers for:
    - `createComponent`: `value` is `{ name: string, sourceObjectId?: string }`; create a local component definition from `sourceObjectId ?? operation.objectId`.
    - `createComponentInstance`: `value` is `{ componentId: string, targetObjectId?: string, variantId?: string }`; create an instance for `targetObjectId ?? operation.objectId` and set that object's `componentInstanceId`.
    - `updateComponentOverride`: `value` is `{ instanceId: string, variantId?: string, state?: "default" | "hover" | "pressed" | "disabled", overrides?: Record<string, unknown> }`; validate the instance and merge fields.
    - `detachComponentInstance`: `value` is `{ instanceId: string }`; mark the instance detached and keep the object in place.
    Add `export * from "./canvas-components.js";` to `packages/editor-core/src/index.ts`.
    Update `packages/editor-core/src/handoff.ts` so `createHandoff` includes these strings in `includes` after `"EditGraph"` and before `"patches"`: `"canvasGraph"`, `"canvasOperations"`, `"components"`, and `"componentInstances"`.
    Create tests for component definition creation, slot ids, default props/variants, invalid variant rejection, override merge, summary output, detach-compatible instance records, and handoff includes.
  </action>
  <acceptance_criteria>
    - packages/editor-core/src/canvas-components.ts exists
    - packages/editor-core/src/canvas-components.ts contains "export function createLocalComponentDefinition("
    - packages/editor-core/src/canvas-components.ts contains "export function createLocalComponentInstance("
    - packages/editor-core/src/canvas-components.ts contains "Emphasis"
    - packages/editor-core/src/canvas-operations.ts contains "createComponentInstance"
    - packages/editor-core/src/handoff.ts contains "\"canvasGraph\""
    - packages/editor-core/src/handoff.ts contains "\"componentInstances\""
    - packages/editor-core/src/index.ts contains "export * from \"./canvas-components.js\""
    - packages/editor-core/src/__tests__/canvas-components.test.ts contains "creates a local component with slots props and variants"
    - packages/editor-core/src/__tests__/handoff.test.ts contains "includes canvas and component records for portable handoff"
    - pnpm --filter @kdesign/editor-core test -- src/__tests__/canvas-components.test.ts src/__tests__/handoff.test.ts exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] `ProjectBundle` accepts optional `canvasGraph` and persisted `canvasOperations`.
- [ ] v1 bundles without canvas data load through a deterministic derived canvas graph.
- [ ] Canvas operations are typed, validated, replayable, and append to stored state.
- [ ] Layout constraints can materialize to safe stored HTML styles for export and preview.
- [ ] Local component definitions and instances support slots, props, variants, overrides, state, and detach records.
- [ ] Agent handoff packages include canvas and component state without becoming runtime-specific.
- [ ] Invalid object ids, locked mutations, stale parents, and unsafe layout values are rejected by tests.
- [ ] Missing component ids, invalid variants, empty override keys, and stale component references are rejected by tests.
- [ ] All task acceptance criteria verified
- [ ] /sunco:lint passes with zero errors
- [ ] npx tsc --noEmit passes with zero errors
- [ ] pnpm lint exits 0
- [ ] pnpm typecheck exits 0
- [ ] pnpm --filter @kdesign/editor-core test -- src/__tests__/canvas-graph.test.ts src/__tests__/canvas-operations.test.ts src/__tests__/canvas-components.test.ts src/__tests__/schemas.test.ts src/__tests__/export.test.ts src/__tests__/handoff.test.ts exits 0
</done_when>
