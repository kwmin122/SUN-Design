---
phase: 6
plan: 02
title: Canvas Layer Tree and Component Workflow
wave: 2
depends_on: [06-01]
files_modified:
  - apps/web/components/editor-shell.tsx
  - apps/web/components/canvas-layer-tree.tsx
  - apps/web/components/canvas-object-inspector.tsx
  - apps/web/components/component-instance-panel.tsx
  - apps/web/components/preview-frame.tsx
  - apps/web/app/globals.css
  - apps/web/tests/phase-06-canvas.spec.ts
  - apps/web/tests/phase-06-components.spec.ts
requirements_addressed:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03
  - CANVAS-04
  - CANVAS-05
gap_closure: false
---

<objective>
Expose the Phase 06 canvas object model through a real web workflow: visible layer tree, object selection, rename/hide/lock/reorder/group controls, object breadcrumb, layout constraints, snapping/guide surface, local component instance controls, and reload persistence.

Complexity: L
Requirements fulfilled: CANVAS-01, CANVAS-02, CANVAS-03, CANVAS-04, CANVAS-05
Phase goal contribution: This turns the core model from Plan 06-01 into the product-facing Paper/Figma-style object surface required by Phase 06.
</objective>

<tasks>
<task id="6-02-01" type="auto">
  <name>Wire canvas graph state into the editor shell</name>
  <files>
    - apps/web/components/editor-shell.tsx
    - apps/web/components/preview-frame.tsx
  </files>
  <read_first>
    - .planning/phases/06-canvas-and-component-model/06-CONTEXT.md
    - .planning/phases/06-canvas-and-component-model/06-RESEARCH.md
    - .planning/phases/06-canvas-and-component-model/06-01-PLAN.md
    - CLAUDE.md
    - .planning/PRODUCT-NORTH-STAR.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - docs/guides/coding-principles.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/canvas-graph.ts
    - packages/editor-core/src/canvas-operations.ts
    - apps/web/components/editor-shell.tsx
    - apps/web/components/preview-frame.tsx
    - apps/web/lib/local-project-store.ts
  </read_first>
  <action>
    Update `apps/web/components/editor-shell.tsx` to import `applyCanvasOperationToBundle`, `deriveCanvasGraph`, `ensureCanvasGraph`, `findCanvasObjectByNodeId`, `listCanvasObjects`, and type `CanvasObject`, `CanvasOperation` from `@kdesign/editor-core`.
    Add state `const [selectedObjectId, setSelectedObjectId] = useState<string | null>(null);`.
    Add derived values:
    - `const canvasGraph = projectBundle ? projectBundle.canvasGraph ?? deriveCanvasGraph(projectBundle) : undefined;`
    - `const selectedObject = selectedObjectId && canvasGraph ? canvasGraph.objects[selectedObjectId] : undefined;`
    - `const selectedNode = selectedObject?.nodeId && projectBundle ? projectBundle.editGraph.nodes[selectedObject.nodeId] : selectedNodeId && projectBundle ? projectBundle.editGraph.nodes[selectedNodeId] : undefined;`
    Change node selection handling so `handleNodeSelected` finds the canvas object by `node.nodeId`, sets `selectedObjectId` to that object id, and still sets `selectedNodeId` for existing inspector compatibility.
    Add helper `commitCanvasOperation(op, value, objectId = selectedObjectId)` that builds a `CanvasOperation` with `id: createLocalId("canvas_op")`, `source: "canvas"`, `baseRevision: current.baseRevision`, `createdAt: new Date().toISOString()`, passes it to `applyCanvasOperationToBundle`, commits the returned bundle with undo tracking, and appends a `patch_rejected` diagnostic if the operation throws.
    Ensure `loadOrCreateBundle` and `commitBundle` store bundles through `ensureCanvasGraph` so reloads always persist a canvas graph.
    Update `apps/web/components/preview-frame.tsx` only if needed to keep overlay geometry parent-owned; do not add same-origin iframe DOM reads.
  </action>
  <acceptance_criteria>
    - apps/web/components/editor-shell.tsx contains "selectedObjectId"
    - apps/web/components/editor-shell.tsx contains "commitCanvasOperation"
    - apps/web/components/editor-shell.tsx contains "findCanvasObjectByNodeId"
    - apps/web/components/editor-shell.tsx contains "ensureCanvasGraph"
    - apps/web/components/preview-frame.tsx does not contain ".contentDocument"
    - pnpm --filter @kdesign/web typecheck exits 0
  </acceptance_criteria>
</task>

<task id="6-02-02" type="auto">
  <name>Create visible layer tree controls</name>
  <files>
    - apps/web/components/canvas-layer-tree.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </files>
  <read_first>
    - .planning/phases/06-canvas-and-component-model/06-CONTEXT.md
    - .planning/phases/06-canvas-and-component-model/06-RESEARCH.md
    - .planning/phases/06-canvas-and-component-model/06-01-PLAN.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/canvas-graph.ts
    - packages/editor-core/src/canvas-operations.ts
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </read_first>
  <action>
    Create `apps/web/components/canvas-layer-tree.tsx` as a client-compatible component with props:
    - `graph: CanvasGraph`
    - `selectedObjectId: string | null`
    - `onSelectObject(objectId: string): void`
    - `onRenameObject(objectId: string, name: string): void`
    - `onSetHidden(objectId: string, hidden: boolean): void`
    - `onSetLocked(objectId: string, locked: boolean): void`
    - `onReorderObject(objectId: string, parentId: string, index: number): void`
    - `onGroupSelection(objectIds: string[]): void`
    - `onUngroupObject(objectId: string): void`
    Render a recursive tree from `graph.rootObjectIds` and `object.childIds`.
    Add `data-testid="canvas-layer-tree"` to the root, `data-testid={"layer-row-" + object.id}` to each row, `data-testid="layer-rename-input"` to the active rename input, and buttons with accessible names `Hide layer`, `Show layer`, `Lock layer`, `Unlock layer`, `Move layer up`, `Move layer down`, `Group selected`, and `Ungroup layer`.
    Update `editor-shell.tsx` to render `CanvasLayerTree` in the left rail below the creation controls or in a dedicated layer section that remains visible when the preview is ready. Hook callbacks to `commitCanvasOperation` using `setObjectName`, `setObjectVisibility`, `setObjectLock`, `reorderObject`, `groupObjects`, and `ungroupObjects`.
    Update `apps/web/app/globals.css` with classes `.layers-panel`, `.layer-row`, `.layer-row.selected`, `.layer-actions`, `.layer-kind`, and responsive rules so the layer tree does not create horizontal body overflow at 768px or 390px viewport widths.
  </action>
  <acceptance_criteria>
    - apps/web/components/canvas-layer-tree.tsx exists
    - apps/web/components/canvas-layer-tree.tsx contains "data-testid=\"canvas-layer-tree\""
    - apps/web/components/canvas-layer-tree.tsx contains "Move layer up"
    - apps/web/components/editor-shell.tsx contains "<CanvasLayerTree"
    - apps/web/app/globals.css contains ".layers-panel"
    - pnpm --filter @kdesign/web typecheck exits 0
  </acceptance_criteria>
</task>

<task id="6-02-03" type="auto">
  <name>Add object inspector layout and guide controls</name>
  <files>
    - apps/web/components/canvas-object-inspector.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
    - apps/web/tests/phase-06-canvas.spec.ts
  </files>
  <read_first>
    - .planning/phases/06-canvas-and-component-model/06-CONTEXT.md
    - .planning/phases/06-canvas-and-component-model/06-RESEARCH.md
    - .planning/phases/06-canvas-and-component-model/06-01-PLAN.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/canvas-operations.ts
    - apps/web/components/editor-shell.tsx
    - apps/web/components/preview-frame.tsx
    - apps/web/tests/phase-02.spec.ts
    - apps/web/tests/phase-04.spec.ts
  </read_first>
  <action>
    Create `apps/web/components/canvas-object-inspector.tsx` with props:
    - `object: CanvasObject | undefined`
    - `nodeKind: string | undefined`
    - `nodeTagName: string | undefined`
    - `onSetName(name: string): void`
    - `onSetLayoutConstraints(constraints: CanvasConstraints): void`
    - `onSetHidden(hidden: boolean): void`
    - `onSetLocked(locked: boolean): void`
    Render root `data-testid="canvas-object-inspector"`.
    When no object is selected, render text `오브젝트를 선택하세요`.
    When selected, render:
    - object breadcrumb text `${object.kind} / ${object.name}`
    - name input with `data-testid="object-name-input"`
    - segmented layout buttons named `Layout block`, `Layout flex`, `Layout grid`
    - editable inputs for gap, padding, grid column count, breakpoint, and width
    - buttons named `Apply gap`, `Apply padding`, `Apply grid columns`, `Apply breakpoint`, `Apply width`, `Align center`, and `Pin top left`
    Each button must call `onSetLayoutConstraints` with values derived from the current inputs where applicable: `{ layout: { display: "flex" } }`, `{ layout: { display: "grid", gridTemplateColumns: "repeat(N, minmax(0, 1fr))" } }`, `{ layout: { gap } }`, `{ layout: { padding } }`, `{ layout: { breakpoint } }`, `{ width }`, `{ layout: { alignItems: "center", justifyContent: "center" } }`, and `{ pinned: { top: true, left: true, right: false, bottom: false } }`.
    Update `editor-shell.tsx` so the existing selection inspector remains available, but the Phase 06 object inspector appears above it and commits `setLayoutConstraints` through `commitCanvasOperation`.
    Add snapping/guide UI hints in the canvas surface: when `selectedObject` exists, render a parent-owned overlay label with `data-testid="canvas-object-breadcrumb"` and a `.snap-guide` line. Do not read from iframe DOM.
    Create `apps/web/tests/phase-06-canvas.spec.ts` with tests that:
    - load `/`, wait for `diagnostics-readiness` to contain `ready`
    - assert `canvas-layer-tree` and `canvas-object-inspector` are visible
    - select an iframe text node, assert `canvas-object-breadcrumb` is visible
    - rename through `object-name-input` and assert localStorage contains the new object name
    - enter grid columns, gap, and breakpoint values; click `Layout grid`, `Apply gap`, `Apply grid columns`, and `Apply breakpoint`; then assert localStorage contains `"gridTemplateColumns":"repeat(4, minmax(0, 1fr))"`, `"breakpoint":"768px"`, and iframe `.feature-grid` style contains `repeat(4` and `--cdx-breakpoint: 768px`
    - reload and assert `canvas-layer-tree` still contains the renamed object
    - set viewport to 768x900 and 390x844 and assert `document.body.scrollWidth <= document.body.clientWidth`
  </action>
  <acceptance_criteria>
    - apps/web/components/canvas-object-inspector.tsx exists
    - apps/web/components/canvas-object-inspector.tsx contains "data-testid=\"canvas-object-inspector\""
    - apps/web/components/canvas-object-inspector.tsx contains "layout-breakpoint-input"
    - apps/web/components/editor-shell.tsx contains "canvas-object-breadcrumb"
    - apps/web/app/globals.css contains ".snap-guide"
    - apps/web/tests/phase-06-canvas.spec.ts contains "document.body.scrollWidth"
    - pnpm e2e -- apps/web/tests/phase-06-canvas.spec.ts exits 0
  </acceptance_criteria>
</task>
<task id="6-02-04" type="auto">
  <name>Add component instance panel and browser workflow</name>
  <files>
    - apps/web/components/component-instance-panel.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
    - apps/web/tests/phase-06-components.spec.ts
  </files>
  <read_first>
    - .planning/phases/06-canvas-and-component-model/06-CONTEXT.md
    - .planning/phases/06-canvas-and-component-model/06-RESEARCH.md
    - .planning/phases/06-canvas-and-component-model/06-01-PLAN.md
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/canvas-graph.ts
    - packages/editor-core/src/canvas-operations.ts
    - packages/editor-core/src/canvas-components.ts
    - packages/editor-core/src/handoff.ts
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </read_first>
  <action>
    Create `apps/web/components/component-instance-panel.tsx` with props:
    - `graph: CanvasGraph`
    - `selectedObject: CanvasObject | undefined`
    - `onCreateComponent(name: string): void`
    - `onCreateInstance(componentId: string): void`
    - `onSetVariant(instanceId: string, variantId: string): void`
    - `onSetState(instanceId: string, state: "default" | "hover" | "pressed" | "disabled"): void`
    - `onSetOverride(instanceId: string, key: string, value: unknown): void`
    - `onDetachInstance(instanceId: string): void`
    Render root `data-testid="component-instance-panel"`.
    Include an input `data-testid="component-name-input"`, an input `data-testid="component-prop-input"`, an input `data-testid="component-variant-input"`, a button named `Create component`, a button named `Create instance`, dynamic variant buttons named `Variant {variant.name}`, state buttons named `State default`, `State hover`, `State pressed`, `State disabled`, an override key input `data-testid="component-override-key-input"`, an override value input `data-testid="component-override-value-input"`, a button named `Apply override`, and a button named `Detach instance`.
    Update `apps/web/components/editor-shell.tsx` to render the panel below the object inspector. Wire callbacks through `commitCanvasOperation`:
    - `createComponent` with `{ name, sourceObjectId: selectedObject.id }`
    - `createComponentInstance` with `{ componentId, targetObjectId: selectedObject.id }`
    - `updateComponentOverride` with `{ instanceId, overrides: { [key]: value } }`
    - variant/state changes through `updateComponentOverride` with `{ instanceId, variantId }` and `{ instanceId, state }`
    - `detachComponentInstance` with `{ instanceId }`
    The web workflow must remain local-project only; do not add publish/remix/token/code-connect UI.
    Add CSS classes `.component-panel`, `.component-controls`, `.component-list`, and responsive wrapping rules.
    Create `apps/web/tests/phase-06-components.spec.ts` to select a canvas object, create a component named `Article Module`, declare a prop named `headline`, create an initial variant named `Marketing`, create an instance, select `Variant Marketing`, set `State hover`, apply override key `headline` with value `가입 CTA`, detach the instance, reload, and assert localStorage contains `"Article Module"`, `"Marketing"`, `"hover"`, `"headline"`, `"가입 CTA"`, `"detached":true`, `"canvasGraph"`, and `"componentInstances"` in handoff/package data.
  </action>
  <acceptance_criteria>
    - apps/web/components/component-instance-panel.tsx exists
    - apps/web/components/component-instance-panel.tsx contains "data-testid=\"component-instance-panel\""
    - apps/web/components/component-instance-panel.tsx contains "component-prop-input"
    - apps/web/components/editor-shell.tsx contains "<ComponentInstancePanel"
    - apps/web/app/globals.css contains ".component-panel"
    - apps/web/tests/phase-06-components.spec.ts contains "Article Module"
    - apps/web/tests/phase-06-components.spec.ts contains "componentInstances"
    - pnpm e2e -- apps/web/tests/phase-06-components.spec.ts exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] The app displays a visible canvas layer tree backed by stored `canvasGraph` data.
- [ ] Selecting an iframe node selects the corresponding canvas object without reading iframe DOM directly.
- [ ] Rename, hide/show, lock/unlock, reorder, group/ungroup, and layout controls commit typed canvas operations.
- [ ] Layout controls persist through reload and materialize to preview/export-safe stored state.
- [ ] A selected object can become a local component with live slots, declared props, variants, validated overrides, and state.
- [ ] A component instance can be created, adjusted, detached, persisted, and reloaded.
- [ ] Desktop, tablet, and mobile browser checks have no horizontal body overflow.
- [ ] Phase 07-only features such as token governance, publish/remix, Storybook/GitHub links, and Code Connect are not implemented in this phase.
- [ ] All task acceptance criteria verified
- [ ] /sunco:lint passes with zero errors
- [ ] npx tsc --noEmit passes with zero errors
- [ ] pnpm lint exits 0
- [ ] pnpm typecheck exits 0
- [ ] pnpm e2e -- apps/web/tests/phase-06-canvas.spec.ts exits 0
- [ ] pnpm e2e -- apps/web/tests/phase-06-components.spec.ts exits 0
</done_when>
