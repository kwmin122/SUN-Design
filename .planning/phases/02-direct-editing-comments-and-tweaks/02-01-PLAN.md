---
phase: 2
plan: 01
title: "Build direct editing, comments, versions, and persisted Tweaks"
wave: 1
depends_on: []
files_modified:
  - packages/editor-core/src/schemas.ts
  - packages/editor-core/src/preview-schemas.ts
  - packages/editor-core/src/patches.ts
  - packages/editor-core/src/index.ts
  - packages/editor-core/src/__tests__/patches.test.ts
  - packages/preview-runtime/src/bridge.ts
  - packages/preview-runtime/src/validation.ts
  - packages/preview-runtime/src/__tests__/validation.test.ts
  - apps/web/lib/preview-message-handler.ts
  - apps/web/components/preview-frame.tsx
  - apps/web/components/editor-shell.tsx
  - apps/web/app/globals.css
  - apps/web/tests/phase-01.spec.ts
  - apps/web/tests/phase-02.spec.ts
requirements_addressed:
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - EDIT-04
  - EDIT-05
  - EDIT-06
  - EDIT-07
  - EDIT-08
  - TWK-01
  - TWK-02
  - TWK-03
  - TWK-04
  - TWK-05
gap_closure: false
---

<objective>
Implement a complete Phase 02 vertical slice: the sandbox iframe reports selectable nodes and geometry, the parent renders the selection overlay and inspector, edits are applied as typed patches to stored normalized HTML, and comments, saved versions, undo/redo, and persisted Tweaks survive reload. This makes generated output directly editable as constrained PPT/Figma/Paper-style canvas objects while preserving the Phase 01 security boundary.

Requirements fulfilled: EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05, EDIT-06, EDIT-07, EDIT-08, TWK-01, TWK-02, TWK-03, TWK-04, TWK-05
Complexity: L
</objective>

<tasks>
<task id="2-01-01" type="auto">
  <name>Extend schemas and patch application</name>
  <files>
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/preview-schemas.ts
    - packages/editor-core/src/patches.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/patches.test.ts
  </files>
  <action>
    Add durable schemas for comments, versions, tweak values, preview node geometry, and Phase 02 edit operations. Create a patch application module that parses normalized HTML with parse5, locates nodes by `data-cdx-id`, applies safe operations only, rebuilds the edit graph, and appends patches without saving live iframe DOM. Supported operations are `setText`, `setStyle`, `replaceAsset`, `setAttr`, `move`, `resize`, `align`, `reorder`, and `setVisibility`.
  </action>
  <acceptance_criteria>
    - ProjectBundleSchema accepts comments, versions, and tweakValues.
    - Patch application refuses unknown node IDs and unsafe attributes.
    - `setText`, `setStyle`, `move`, `resize`, `align`, and `setVisibility` update normalized HTML deterministically.
    - Unit tests cover successful patches and negative patch validation.
  </acceptance_criteria>
</task>

<task id="2-01-02" type="auto">
  <name>Add validated node registry and selection bridge messages</name>
  <files>
    - packages/editor-core/src/preview-schemas.ts
    - packages/preview-runtime/src/bridge.ts
    - packages/preview-runtime/src/validation.ts
    - packages/preview-runtime/src/__tests__/validation.test.ts
    - apps/web/lib/preview-message-handler.ts
  </files>
  <action>
    Extend the preview bridge to post a node registry with rects, roles, tag names, and short text previews. Add click and pointerover handlers that post selected/hovered node IDs. Extend parent-side validation with the same source, nonce, type, and schema checks as Phase 01. Preserve all negative bridge tests.
  </action>
  <acceptance_criteria>
    - Known message types include `preview.nodes`, `preview.select`, and `preview.hover`.
    - Invalid source, missing nonce, wrong nonce, unknown type, and schema-invalid payloads still fail.
    - Runtime bridge script does not access parent app state, cookies, or storage.
  </acceptance_criteria>
</task>

<task id="2-01-03" type="auto">
  <name>Build parent-owned overlay, inspector, comments, versions, and tweaks UI</name>
  <files>
    - apps/web/components/preview-frame.tsx
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </files>
  <action>
    Render a parent-owned overlay around the selected iframe node. Add a selected-node inspector in the Tweaks rail with text editing, color/background/font/radius/visibility controls, move/resize/align/reorder controls, comment capture, save-version, undo, and redo. Keep all controls stable and accessible with labels/test IDs. Persist changes through the bundle store.
  </action>
  <acceptance_criteria>
    - Clicking editable content in the iframe selects that node and shows an aligned overlay.
    - Inspector edits update the preview and patch log immediately.
    - Comments and versions appear in the UI and survive reload.
    - Undo/redo controls operate on stored bundle snapshots.
    - Global Tweaks remain live and persisted.
  </acceptance_criteria>
</task>

<task id="2-01-04" type="auto">
  <name>Verify every visible Phase 02 path</name>
  <files>
    - apps/web/tests/phase-01.spec.ts
    - apps/web/tests/phase-02.spec.ts
    - .planning/phases/02-direct-editing-comments-and-tweaks/02-01-SUMMARY.md
    - .planning/phases/02-direct-editing-comments-and-tweaks/02-VERIFICATION.md
  </files>
  <action>
    Add browser tests for selecting a headline, editing text, styling selected nodes, layout controls, comments, saved versions, undo/redo, reload persistence, toolbar tabs/modes, Tweaks toggle, and bridge spoof rejection. Run lint, tests, typecheck, e2e, and visual screenshot smoke. Record Computer Use status; use browser automation fallback if macOS blocks desktop automation.
  </action>
  <acceptance_criteria>
    - `pnpm lint` exits 0.
    - `pnpm test` exits 0.
    - `pnpm typecheck` exits 0.
    - `pnpm e2e` exits 0.
    - Phase 02 verification document records all command results and UI path coverage.
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- All Phase 02 requirements are implemented or explicitly tied to the narrow constrained direct-manipulation subset.
- The source of truth is stored bundle state, not live iframe DOM.
- The app can be reloaded after direct edits and preserve selected text/style/comment/version/tweak results.
- Browser tests cover the user-visible controls the vibe-coder persona will touch first.
</done_when>
