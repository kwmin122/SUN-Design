---
phase: 3
plan: 01
title: "Add deterministic prompt creation, context attachments, and Korean presets"
wave: 1
depends_on: []
files_modified:
  - packages/editor-core/src/schemas.ts
  - packages/editor-core/src/generation.ts
  - packages/editor-core/src/index.ts
  - packages/editor-core/src/__tests__/generation.test.ts
  - apps/web/components/editor-shell.tsx
  - apps/web/app/globals.css
  - apps/web/tests/phase-03.spec.ts
requirements_addressed:
  - CORE-01
  - CORE-06
  - CORE-07
  - CORE-08
  - CORE-09
  - CORE-10
  - SEC-04
  - KOR-01
  - KOR-02
gap_closure: false
---

<objective>
Implement Phase 03 as a deterministic local generation layer: prompt, creation mode, fidelity, context attachments, web capture placeholders, asset manifest status, and Korean presets create stored generated bundles that immediately render in the sandbox and remain editable through Phase 02 controls.
</objective>

<tasks>
<task id="3-01-01" type="auto">
  <name>Create generation schemas and deterministic templates</name>
  <files>
    - packages/editor-core/src/schemas.ts
    - packages/editor-core/src/generation.ts
    - packages/editor-core/src/index.ts
    - packages/editor-core/src/__tests__/generation.test.ts
  </files>
  <action>
    Extend bundle source metadata for generated artifacts and create deterministic Korean-first mock generation helpers. Include modes, fidelity, context attachments, asset manifest entries, and three presets.
  </action>
  <acceptance_criteria>
    - Generated bundles parse with `ProjectBundleSchema`.
    - Source metadata records prompt, mode, fidelity, and preset.
    - Generated HTML has stable `data-cdx-id` after normalization.
    - Asset manifest entries have explicit status.
    - Unit tests cover presets and context asset mapping.
  </acceptance_criteria>
</task>

<task id="3-01-02" type="auto">
  <name>Wire prompt UI, mode controls, attachments, and presets</name>
  <files>
    - apps/web/components/editor-shell.tsx
    - apps/web/app/globals.css
  </files>
  <action>
    Make the existing Claude Design-like left rail controls functional for local deterministic creation. Add attachment chips, preset buttons, source/asset status UI, and generated-bundle handoff into the existing commit/persistence path.
  </action>
  <acceptance_criteria>
    - Mode tabs and fidelity buttons visibly update state.
    - Attachment buttons add source-material chips and manifest entries.
    - Send creates a generated bundle that renders in the iframe.
    - Preset buttons create the three Korean artifact contexts.
    - The generated artifact can still be selected and edited.
  </acceptance_criteria>
</task>

<task id="3-01-03" type="auto">
  <name>Verify generated artifact paths</name>
  <files>
    - apps/web/tests/phase-03.spec.ts
    - .planning/phases/03-prompt-creation-modes-assets-and-korean-presets/03-01-SUMMARY.md
    - .planning/phases/03-prompt-creation-modes-assets-and-korean-presets/03-VERIFICATION.md
  </files>
  <action>
    Add E2E coverage for prompt generation, mode/fidelity, context attachments, web capture placeholder, asset manifest, Korean presets, reload persistence, and direct editing of generated output.
  </action>
  <acceptance_criteria>
    - `pnpm lint` exits 0.
    - `pnpm test` exits 0.
    - `pnpm typecheck` exits 0.
    - `pnpm e2e` exits 0.
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- Prompt-created artifacts enter the same safe persisted editing pipeline.
- No real AI provider, scraping, upload, or hosted storage is introduced.
- Korean preset coverage is visible and tested.
</done_when>
